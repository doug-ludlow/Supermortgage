/**
 * §18.7 tools — the spec's tool strings for process 18.7, verbatim, via
 * `defineTools("18.7", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section18.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_18_7: readonly ToolDef[] = [];

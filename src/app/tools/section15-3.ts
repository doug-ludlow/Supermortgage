/**
 * §15.3 tools — the spec's tool strings for process 15.3, verbatim, via
 * `defineTools("15.3", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section15.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_15_3: readonly ToolDef[] = [];

/**
 * §28.3 timer overrides (process-owned; applied after every section's so they win the merge — see
 * src/domain/bankruptcy/timers-14-1.ts for the pattern). One `reg.override(code, { trigger?, satisfied | evaluator,
 * anchorField?, offset?, why })` per 28.3 registry row whose trigger/satisfied column names an event the platform
 * spells differently or a condition the column grammar drops; `why` quotes the spec. A code the servicing spec
 * (sections 1–19) owns is never overridden here — reference it. Wired by src/domain/timer-overrides.ts.
 *
 * Emitters live in src/domain/qc-hmda/ops-28-3.ts (the `hmda` agent's rules): `hmda.action_taken.recorded{quarter_end,
 * lar_entry_due_on}` (finalizeActionTaken / applyAdverseAction — 21.6's event name, 28.3's anchor fields), `hmda.record.finalized`
 * and `hmda.completeness.checked` / `hmda.completeness.failed` (runCompletenessJob), `schedule.tick{cadence, job}`
 * (dailyCompletenessTick / coverageTestTick / quarterEndTick / modifiedLarNoticeTick), `hmda.coverage.determined{covered,
 * filing_deadline}` (recordCoverageDecision), `hmda.lar.built{edits_target_on}` (buildLarFile), `hmda.lar.macro_verified
 * {verified_on}` (verifyMacroEdits), `hmda.lar.signed` / `hmda.lar.accepted{kind}` (signSubmission), `hmda.ffiec.disclosure_notice
 * .received{received_on}` (receiveFfiecDisclosureNotice), `hmda.public_notice.made_available{kind}` (makePublicNoticeAvailable),
 * `hmda.public_notice.attested` (attestNoticeAvailability). Referenced, never redefined: `HMDA_1003_5_LAR_RETENTION_3Y` (31.3's
 * not_before_gate on disposal — it arms on this process's `hmda.lar.accepted{signed_at}`; the former 28.3 alias
 * `HMDA_1003_5A_LAR_RETENTION_3Y` is retired).
 */
import type { TimerRegistry } from "../../kernel/timers/registry.ts";

export function applySatisfiedOverrides_28_3(reg: TimerRegistry): void {
  const o = reg.override.bind(reg);
  // Rule 1 / rule 14 / T9–T10: the annual deadline exists only for a covered partner — it arms on the officer's coverage decision (covered=true) and is anchored on the Mar 1 deadline the decision carries (2026 data → Mon Mar 1, 2027); an uncovered partner arms nothing.
  o("HMDA_1003_5_ANNUAL_0301", { trigger: "`hmda.coverage.determined{covered=true}`", anchorField: "filing_deadline", offset: "0 calendar_days", satisfied: "`hmda.lar.accepted{kind∈{annual, resubmission}, status_code=15}`",
    why: "§28.3 timer table: trigger 'Jan 1 of year Y+1 for records with `reporting_year = Y` (only when `hmda_coverage_tests.covered = true`)', anchor 'Mar 1 of Y+1', '0 `calendar_days` (no weekend/holiday adjustment stated in the rule; policy: sign by Feb 20)', satisfied by '`hmda.lar.accepted` (status 15) with the officer's `signed_at` and receipt', breach 'sev 1 → partner `officer`; file immediately; incident record; late-filing log for the exam'. §1003.5(a)(1)(i): 'By March 1 following the calendar year for which data are collected'. ops-28-3.ts recordCoverageDecision appends `hmda.coverage.determined{covered, filing_deadline}` (the officer's act, T10: 0/900 → covered=false arms nothing; 310/1,140 → covered=true, deadline 2027-03-01); signSubmission appends `hmda.lar.signed` and `hmda.lar.accepted{status_code=15, signed_at, receipt}` under the officer's token (T9: signed Tue Jan 12, 2027 — 48 days early)." });
  // Rule 14: quarterly only for a ≥ 60,000-record reporter — the quarter-end tick carries `quarterly_reporter` and the quarter-end anchor; +60 calendar days (Q1 2027 → Sun May 30; Q2 → Sun Aug 29; Q3 → Mon Nov 29, 2027).
  o("HMDA_1003_5_QUARTERLY_60", { trigger: "`schedule.tick{cadence=quarterly, job=hmda_quarterly_lar, quarterly_reporter=true}`", anchorField: "quarter_end", offset: "+60 calendar_days", satisfied: "`hmda.lar.accepted{kind=quarterly}`",
    why: "§28.3 timer table: trigger 'quarter-end (Q1–Q3) when `quarterly_reporter = true`', anchor 'last day of the quarter', '+60 `calendar_days`', satisfied by 'quarterly `hmda.lar.accepted`', breach 'sev 1'. §1003.5(a)(1)(ii): institutions that 'reported for the preceding calendar year at least 60,000 covered loans and applications, combined' submit 'Within 60 calendar days after the end of each calendar quarter except the fourth quarter'. ops-28-3.ts quarterEndTick appends `schedule.tick{cadence=quarterly, job=hmda_quarterly_lar, quarter_end, quarterly_reporter}`; coverageTest sets quarterly_reporter from preceding_year_total_records ≥ 60,000; buildLarFile(kind=quarterly) + signSubmission → `hmda.lar.accepted{kind=quarterly}`." });
  // Rule 13 / T7: §1003.4(f) — 30 calendar days after the end of the calendar quarter of final action; the 28.3 emission of 21.6's event name carries the quarter-end anchor (Nov 6, 2026 → Q4 end Dec 31 → Sat Jan 30, 2027); the daily job's `hmda.record.finalized` (completeness_status = complete) satisfies it.
  o("HMDA_1003_4F_LAR_ENTRY_Q30", { trigger: "`hmda.action_taken.recorded{quarter_end present}`", anchorField: "quarter_end", offset: "+30 calendar_days", satisfied: "`hmda.record.finalized{completeness_status=complete}`",
    why: "§28.3 timer table: trigger 'final action (`hmda.action_taken.recorded`, `loan.purchased` same-year sale)', anchor 'last day of the calendar quarter of final action', '+30 `calendar_days`', satisfied by '`hmda_records.completeness_status = complete`', breach 'sev 2 → `compliance-sentinel`; the daily job lists the ULIs'. §1003.4(f): 'record the data … on a loan/application register within 30 calendar days after the end of the calendar quarter in which final action is taken'. ops-28-3.ts finalizeActionTaken (code 1 on `loan.funded` after rescission expired) and applyAdverseAction (21.6's codes 2–5) append `hmda.action_taken.recorded{action_taken, action_taken_date, quarter_end, lar_entry_due_on}` — the `quarter_end` field marks 28.3's finalising emission (21.6's own emission at the disposition carries no anchor); runCompletenessJob appends `hmda.record.finalized{completeness_status=complete}` when no required field is missing and the local S/V edits pass (worked: Fri Nov 20, 2026, the day after the purchase set purchaser_type = 1), and `hmda.completeness.failed` at sev 2 for a record still incomplete past the due date (T7: Jan 31, 2027)." });
  // Rule 13: the 02:00 partner-time daily run — recurring from the scheduler's tick, re-armed by each job run.
  o("SM_HMDA_LAR_COMPLETENESS_DAILY", { trigger: "`schedule.tick{cadence=daily, job=hmda_lar_completeness}`", anchorField: "date", offset: "+1 calendar_days", satisfied: "`hmda.completeness.checked`",
    why: "§28.3 timer table: recurring on 'schedule', anchor '02:00 partner TZ daily', 'daily; `calendar_days`', satisfied by 'job run with results (`hmda.completeness.failed` per record)', breach 'missed run → sev 2'. ops-28-3.ts dailyCompletenessTick appends `schedule.tick{cadence=daily, job=hmda_lar_completeness, date}` (02:00 America/Phoenix); runCompletenessJob appends the run's results as `hmda.completeness.checked{date, records, complete, incomplete, failed, ulis_overdue}` plus one `hmda.completeness.failed{uli, missing_fields}` per overdue record — the checked fact is the 'job run with results' that satisfies the row (the recurring row re-arms from it for the next day)." });
  // Rule 1 / T10: Jan 2 tick; the officer's decision must land by Jan 31 (default to 'covered' — file rather than miss).
  o("HMDA_1003_2G_COVERAGE_TEST_ANNUAL", { trigger: "`schedule.tick{cadence=annual, job=hmda_coverage_test}`", anchorField: "date", offset: "+29 calendar_days", satisfied: "`hmda.coverage.determined`",
    why: "§28.3 timer table: recurring on 'schedule', anchor 'Jan 2 each year', 'annual', satisfied by '`hmda.coverage.determined` signed by the officer', breach 'sev 2; default to \"covered\" if undecided by Jan 31 (file rather than miss)'. §1003.2(g)(2): '(i) On the preceding December 31, had a home or branch office in an MSA; and (ii)(A) In each of the two preceding calendar years, originated at least 25 closed-end mortgage loans'. ops-28-3.ts coverageTestTick appends `schedule.tick{cadence=annual, job=hmda_coverage_test, date, decide_by}` (Jan 2 → +29 calendar days = Jan 31, the decision date the breach column names); coverageTest computes covered/quarterly_reporter with the basis; recordCoverageDecision refuses any actor but a human `officer` and appends `hmda.coverage.determined`." });
  // Rule 14: Jan 5 build → clean edits (status ≥ 8 with every Q/M edit explained = status 14) by Feb 1 (the build event carries `edits_target_on`).
  o("SM_HMDA_PRE_SUBMISSION_EDITS_0201", { trigger: "`hmda.lar.built{kind∈{annual, resubmission}}`", anchorField: "edits_target_on", offset: "0 calendar_days", satisfied: "`hmda.lar.macro_verified{status_code=14}`",
    why: "§28.3 timer table: internal deadline on 'Jan 5 build', anchor 'Feb 1', '0 `calendar_days`', satisfied by 'platform status ≥ 8 with all Q/M edits explained', breach 'sev 2 → partner `officer`'. ops-28-3.ts buildLarFile appends `hmda.lar.built{kind, lar_row_count, sha256, edits_target_on}` (edits_target_on = Feb 1 of Y+1); receiveEdits records the platform's S/V/Q/M edits (S/V → records `edits_failed`, corrected at source via correctAtSource and rebuilt as a resubmission); explainEdit stores every Q/M explanation (hmda_edit_verifications); verifyQualityEdits then verifyMacroEdits append `hmda.lar.quality_verified` and `hmda.lar.macro_verified{status_code=14}` — status 14 'Ready for submission' is exactly 'status ≥ 8 with all Q/M edits explained' (worked: Jan 8, 2027)." });
  // Policy: 5 creditor business days from status 14, never later than Mar 1 (Jan 8 → Fri Jan 15, 2027); the officer's own `hmda.lar.signed` satisfies it.
  o("SM_HMDA_OFFICER_SIGN_SLA_5BD", { trigger: "`hmda.lar.macro_verified{status_code=14}`", anchorField: "verified_on", offset: "+5 business_days_creditor", satisfied: "`hmda.lar.signed`",
    why: "§28.3 timer table: trigger '`hmda.lar.macro_verified` (status 14)', anchor 'verification time', '+5 `business_days_creditor` (and never later than Mar 1)', satisfied by '`hmda.lar.signed`', breach 'sev 1 → partner `officer`'. ops-28-3.ts verifyMacroEdits appends `hmda.lar.macro_verified{status_code=14, verified_on, sign_due_on}` (officerSignDue caps the due date at the filing deadline); signSubmission — refused for any actor but a human `officer` (the /sign call runs under the officer's Login.gov token) — appends `hmda.lar.signed{signed_by, signed_at}` (T9: status 14 Jan 8 → due Fri Jan 15; signed Tue Jan 12, 2027)." });
  // Rule 16 / T13: +3 creditor business days from the FFIEC availability notice (Wed Jun 16 → Mon Jun 21, 2027); the (b)(2) notice at the home office and MSA/MD branches satisfies it.
  o("HMDA_1003_5B_DISCLOSURE_NOTICE_3BD", { trigger: "`hmda.ffiec.disclosure_notice.received`", anchorField: "received_on", offset: "+3 business_days_creditor", satisfied: "`hmda.public_notice.made_available{kind=b2}`",
    why: "§28.3 timer table: trigger 'FFIEC disclosure-statement availability notice received', anchor 'receipt date', '+3 `business_days_creditor` (unit not defined by Reg C — policy)', satisfied by '`hmda.public_notice.made_available{b2}` at the home office and MSA/MD branches', breach 'sev 2 → partner `officer`'. §1003.5(b)(2): 'No later than three business days after receiving notice from the FFIEC that a financial institution's disclosure statement is available, the financial institution shall make available to the public upon request at its home office, and each branch office physically located in each MSA and each MD, a written notice'. ops-28-3.ts receiveFfiecDisclosureNotice appends `hmda.ffiec.disclosure_notice.received{received_on, due_on}`; makePublicNoticeAvailable(kind=disclosure_statement_notice_b2) appends `hmda.public_notice.made_available{kind=b2, made_available_on, available_until}` with available_until = +5 years (T13: 2032-06-21)." });
  // Rule 16: Jan 1 following the data year; policy availability by Mar 31 (Jan 1 + 89 calendar days) — the (c)(1) modified-LAR notice satisfies it.
  o("HMDA_1003_5C_MODIFIED_LAR_NOTICE_ANNUAL", { trigger: "`schedule.tick{cadence=annual, job=hmda_modified_lar_notice}`", anchorField: "date", offset: "+89 calendar_days", satisfied: "`hmda.public_notice.made_available{kind=c1}`",
    why: "§28.3 timer table: recurring on 'Jan 1 following the data year', anchor 'Jan 1 (policy: by Mar 31, matching the CFPB's modified-LAR publication practice — [UNVERIFIED])', 'annual', satisfied by '`hmda.public_notice.made_available{c1}`', breach 'sev 2'. §1003.5(c): a written notice that the modified LAR 'may be obtained on the Bureau's Web site', made available 'following the calendar year for which the data are collected'. ops-28-3.ts modifiedLarNoticeTick appends `schedule.tick{cadence=annual, job=hmda_modified_lar_notice, date, available_by}` (Jan 1 + 89 calendar days = Mar 31 — 28.3-Q6 policy); makePublicNoticeAvailable(kind=modified_lar_notice_c1) appends `hmda.public_notice.made_available{kind=c1}` with available_until = +3 years." });
  // Rule 16: the 3-year (c1) / 5-year (b2) availability window is a condition over the notice's facts — attested annually by the office manager.
  o("HMDA_1003_5D_NOTICE_AVAILABILITY", { trigger: "`hmda.public_notice.made_available{kind∈{b2, c1}}`", anchorField: "made_available_on", evaluator: "28.3.noticeAvailabilityWindow", satisfied: "`hmda.public_notice.attested`",
    why: "§28.3 timer table: window on 'notice made available', anchor `made_available_at`, '3 years (c1) / 5 years (b2) `calendar_days`', satisfied by 'annual attestation by the office manager', breach 'sev 2'. §1003.5(d): the (c) notice 'for a period of three years and the notice required by paragraph (b)(2) … for a period of five years … during the hours the office is normally open to the public for business'. evaluators-28-3 noticeAvailabilityWindow over {made_available_on, available_until, last_attested_on, as_of} (ops-28-3.ts): open while the notice is inside its window and attested within the last 12 months, open once the window has closed, closed (sev 2) when the attestation is stale; attestNoticeAvailability appends `hmda.public_notice.attested{kind, year, attested_on}` (the office manager's signed attestation with photos)." });
}

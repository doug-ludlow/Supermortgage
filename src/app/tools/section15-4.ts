/**
 * §15.4 tools — the spec's tool strings for process 15.4, verbatim, via
 * `defineTools("15.4", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section15.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_15_4: readonly ToolDef[] = [];

// 24.4 Title, vesting, settlement-agent vetting, existing-lien payoffs, subordinations, and curative
// spec/sections/24-property-valuation-eligibility-title-hazard-flood-insurance/24-4-title-vesting-settlement-agent-vetting-existing-lien-payoffs.md
// One node:test per T-id, named exactly as the spec. `todo: true` = not implemented yet (tools/audit.py does not
// count it). Implement by replacing the todo line with a real test; never edit the name.
import { test } from "node:test";

test("24.4-T1: Given the refinance fixture (application Oct 5, 2026) and a commitment proposing the \"ALTA Loan Policy (6-17-06)\", when `FNMA_B7_2_01_TITLE_EVIDENCE_GATE` is evaluated on Nov 10, 2026, then the gate fails with reason `policy_form_not_2021` and a curative item addressed to the agent; a corrected pro forma on the 2021 form passes.", { todo: true });
test("24.4-T2: Given a commitment for the Phoenix property (1-unit, fixed rate, not condo/PUD) listing endorsements {none}, when endorsements are computed, then `required_endorsements = [ALTA 8.1]` and the gate fails until 8.1 is committed; given the same property on a 7/6 ARM, then `[ALTA 8.1, ALTA 6]`.", { todo: true });
test("24.4-T3: Given a condo unit with a commitment lacking ALTA 4/4.1, when reviewed, then a blocking curative item opens; given the endorsement issued as \"ALTA 4.1-06\", then the item clears.", { todo: true });
test("24.4-T4: Given a title insurer not licensed in Arizona per the state DOI lookup, when the commitment is parsed, then the order is rejected with reason `insurer_not_licensed` and the agent is asked to re-issue under a licensed underwriter.", { todo: true });
test("24.4-T5: Given an external payoff statement good through Nov 12, 2026 and a planned disbursement Thu Nov 12, then `SM_PAYOFF_GOOD_THROUGH_GATE` passes; when disbursement moves to Fri Nov 13, then the statement becomes `stale`, a refresh is requested, and `disburse` is refused until the refreshed statement (good through \u2265 Nov 13) arrives.", { todo: true });
test("24.4-T6: Given the same-servicer case with payoff posted Nov 12, 2026 and an escrow balance of $2,347.18 and no consent, then servicing 3.5's refund is due Fri Dec 11, 2026; given the `escrow_credit_to_new_loan` consent signed Nov 6, then the CD's initial escrow deposit is reduced from $2,912.50 to $565.32 and no refund check is issued.", { todo: true });
test("24.4-T7: Given a HELOC ($50,000 line, $18,000 drawn) staying in place on the $560,000/$800,000 refinance, then `cltv_bps=7225`, `hcltv_bps=7625`; when no executed resubordination exists on Nov 5, 2026, then `consummate` is refused by `FNMA_B2_1_2_04_RESUBORDINATION_GATE`; when an agreement with a balloon 3 years after the note date is received, then `terms_ok=false` and the lien must be paid off.", { todo: true });
test("24.4-T8: Given verified wire instructions dated Oct 30, 2026, when new instructions arrive by e-mail on Nov 11 at 3:00 p.m. (funding Nov 12), then the wire is blocked, the vendor check returns `changed`, and only a `funding_approver` release after a second callback to the ALTA-Registry number can unblock it.", { todo: true });
test("24.4-T9: Given a cash-out refinance with a POA presented by the borrower's spouse, when reviewed, then `result='ineligible'` (transaction type) unless `applicable_law_override` with a written file statement; given a limited cash-out with the title agency's employee as attorney-in-fact, then eligibility requires the recorded interactive session and a CPL.", { todo: true });
test("24.4-T10: Given an inter vivos trust whose sole trustee is the settlor's adult child (not institutional), when reviewed, then `result='ineligible'`; given the settlor as co-trustee with the child, then eligible, SFC 168 is set, and the signature plan includes the settlor acknowledgment.", { todo: true });
test("24.4-T11: Given a Texas 50(a)(6) loan requesting an attorney opinion letter, when the AOL path is evaluated, then it is refused (ineligible transaction) and T-2/T-42/T-42.1 are required; given a T-42 with paragraph 2(c) deleted, then the gate fails.", { todo: true });
test("24.4-T12: Given a NY CEMA refinance of $560,000 consolidating $531,240, then `new_money_cents = 2,876,000`, the \u00a7255 affidavit is generated, the existing lender's assignment is a required deliverable, and the mortgage-tax fee item is computed on $28,760 only.", { todo: true });
test("24.4-T13: Given a tax certificate showing the second-half 2026 Maricopa County installment delinquent, when classified, then `to_be_paid` blocks consummation until the settlement statement shows the payment.", { todo: true });
test("24.4-T14: Given a CPL naming a different agent than `settlement_agent_party_id`, when `SM_CPL_BEFORE_FUNDING_GATE` runs, then `disburse` is refused with reason `cpl_agent_mismatch`.", { todo: true });

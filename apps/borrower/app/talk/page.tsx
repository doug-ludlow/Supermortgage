import { Talk } from "@/components/talk/Talk";

export const dynamic = "force-dynamic";

/** /app/talk — the entry as one conversation (src/runtime/borrower/talk.ts): type or speak; the agent does the rest through the same tools the chips use. */
export default function TalkPage() {
  return <Talk />;
}

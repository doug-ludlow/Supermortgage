/**
 * §18.2 tools — the spec's tool strings for process 18.2, verbatim, via
 * `defineTools("18.2", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section18.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_18_2: readonly ToolDef[] = [];

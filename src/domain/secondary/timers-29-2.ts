/**
 * §29.2 timer overrides (process-owned; applied after every section's so they win the merge — see
 * src/domain/bankruptcy/timers-14-1.ts for the pattern). One `reg.override(code, { trigger?, satisfied | evaluator,
 * anchorField?, offset?, why })` per 29.2 registry row whose trigger/satisfied column names an event the platform
 * spells differently or a condition the column grammar drops; `why` quotes the spec. A code the servicing spec
 * (sections 1–19) owns is never overridden here — reference it. Wired by src/domain/timer-overrides.ts.
 *
 * The events named here are appended by src/domain/secondary/ops-29-2.ts (PipelineService): the partner's hedging
 * policy adoption (`hedge.policy.updated{initial=true}` — the program's first version) arms every recurring 29.2 clock
 * (the 07:00 ET position, the 17:00 ET mark, the rate-shock table, the Friday liquidity stress, the monthly fallout
 * review); each clock is satisfied by the artifact it waits for and, being `recurring`, is re-armed from that event
 * (the satisfying payloads carry the next anchor: `clock_anchor_on`, `liquidity_review_on`, `fallout_review_on`).
 * Per-lock, per-position and per-call rows arm on 21.4's `lock.executed`, on `hedge.trade.executed` (aggregate
 * `hedge_position`) and on `margin.call.received` (aggregate `margin_call`). Every 29.2 event carries `source:
 * "origination"` (or the application id), so the rows arm only under origination context.
 */
import type { TimerRegistry } from "../../kernel/timers/registry.ts";

export function applySatisfiedOverrides_29_2(reg: TimerRegistry): void {
  const o = reg.override.bind(reg);
  // Rule 13 / timer table: the 07:00 ET position every Fannie Mae business day; the trigger column is "—" — the clock starts with the partner's policy (the program's prerequisite) and re-arms from every snapshot.
  o("SM_PIPELINE_POSITION_DAILY_0700ET", { trigger: "`hedge.policy.updated{initial=true}`", anchorField: "clock_anchor_on", offset: "+1 business_days_fannie_et, 07:00 ET", satisfied: "`pipeline.snapshot.taken`",
    why: "§29.2 timer table: recurring, anchor '07:00 America/New_York every `business_days_fannie_et` day (plus 12:00 and 16:00 intraday; 12:30 on SIFMA early-close days)', '1 `calendar_days`', satisfied by `pipeline.snapshot.taken`; breach 'sev 2; the 8:15 a.m. ET committing open proceeds on the last snapshot; `officer` informed' — PipelineService.takeSnapshot emits the event with `clock_anchor_on` (its ET date) so the recurring re-arm lands on the next business day 07:00 ET; the first instance arms when the partner's hedging policy (operational prerequisite) is adopted (`hedge.policy.updated{initial=true}`); handleBreach opens the sev 2 officer escalation." });
  // Timer table: the intraday trigger is a price move, not a clock — PipelineService.observePrice emits `pipeline.price_move.detected` at ≥ 12.5 bps; the snapshot that follows carries the coverage check.
  o("SM_PIPELINE_INTRADAY_MOVE_TRIGGER", { trigger: "`pipeline.price_move.detected`", offset: "same day", satisfied: "`pipeline.snapshot.taken{coverage_checked=true}`",
    why: "§29.2 timer table: 'recurring (event)' on 'TBA/whole-loan price move ≥ 12.5 bps vs the last snapshot (matches 20.4's rate-sheet republish threshold)', anchor 'price feed', offset 'immediate', satisfied by '`pipeline.snapshot.taken` + coverage check' — ops-29-2.ts priceMoveTrigger / PipelineService.observePrice emit `pipeline.price_move.detected{move_bps, threshold_bps}`; takeSnapshot emits `pipeline.snapshot.taken{coverage_checked=true, within_band, coverage_outside_band}` the same day." });
  // Rule 3: the band gate is instantiated by a mandatory-mode snapshot whose coverage left [0.85, 1.10]; the evaluator asserts the ratio; the trade restoring the band is confirmed by the coverage check recordTradeConfirmation runs (an intraday snapshot inside the band).
  o("SM_HEDGE_COVERAGE_BAND_GATE", { trigger: "`pipeline.snapshot.taken{coverage_outside_band=true}`", evaluator: "29.2.coverageBand", satisfied: "`pipeline.snapshot.taken{within_band=true}`",
    why: "§29.2 timer table: not_before_gate (band) on 'every snapshot in mandatory mode', anchor '`coverage_ratio = hedge_face × duration_factor ÷ expected_deliverable`', 'must stay within [`coverage_band_low`, `coverage_band_high`] (default 0.85–1.10)', satisfied by '`hedge.trade.executed` restoring the band'; breach 'outside the band → `hedge.rebalance.recommended` the same day; > 20 points outside → `officer` paged within 1 hour; rebalance due by the next `business_days_fannie_et` close' — takeSnapshot flags `coverage_outside_band` (T4: 1.1111 > 1.10) and recommendRebalance issues the package; recordTradeConfirmation re-snapshots after the execution so the restoring trade closes the gate with `within_band=true`; coverageBandGate is the evaluator over {coverage_ratio | hedge_face_cents, duration_factor, expected_deliverable_cents}." });
  // Rule 12 / T11: +5 Fannie Mae business days from the lock, 5:00 p.m. ET (Wed Oct 7 → Thu Oct 15, 2026: Oct 8, 9, 13, 14, 15 across Columbus Day — the spec's 'Wed Oct 14 (Oct 8, 9, 13, 14)' lists four days; the calendar-correct fifth is asserted); 29.1's commitment on the same application satisfies it.
  o("SM_UNCOMMITTED_POSITION_5BD", { trigger: "`lock.executed`", offset: "+5 business_days_fannie_et, 17:00 ET", satisfied: "`commitment.executed`",
    why: "§29.2 timer table: deadline on 'a lock that is neither committed (29.1) nor covered by the hedge program', anchor '`locks.locked_at`', '+5 `business_days_fannie_et`', satisfied by '`commitment.executed` or inclusion in `mandatory_pipeline`'; breach 'sev 2 → partner `officer`; 29.1 economics (DPA vs exposure) re-run daily' — 21.4 emits `lock.executed` with the application id (the lock's ET date anchors the row); 29.1's CommitmentService emits `commitment.executed` on the same application; a lock the hedge program covers is committed mandatory through `hedge.commitment.requested` → `commitment.executed`; ops-29-2.ts uncommittedPositionDue; handleBreach opens the sev 2 officer escalation (T11)." });
  // Rule 9 / T5: anchored on the SIFMA notification date the confirmation carries; −3 business days at 12:00 p.m. ET (Dec 8 → Thu Dec 3, 2026; Jan 12 → Thu Jan 7, 2027); rolled or paired off closes it.
  o("SIFMA_TBA_ROLL_GATE_3BD", { trigger: "`hedge.trade.executed{kind∈{open, roll_open}, instrument∈{tba_umbs_30, tba_umbs_15}}`", anchorField: "notification_date", offset: "-3 business_days_fannie_et, 12:00 ET", satisfied: "`hedge.position.*`",
    why: "§29.2 timer table: deadline on '`hedge.trade.executed{open}` for a TBA position', anchor 'SIFMA notification date of the position's class and settlement month', '−`roll_lead_business_days` (default 3) `business_days_fannie_et`, 12:00 p.m. ET', satisfied by '`hedge.position.rolled` or `hedge.position.paired_off`' (the only `hedge.position.*` events the service emits — both on the position's aggregate); breach 'sev 1 → partner trader and `officer` the same hour; the position must never reach the notification date' — recordTradeConfirmation(kind=open, and kind=roll_open for the back-month leg of a roll) emits the event with `notification_date` and `roll_due_on` (ops-29-2.ts rollDueOn); a `roll_close` (+ `roll_open`) or a full `pair_off` confirmation emits the satisfying event; handleBreach opens the sev 1 officer escalation." });
  // FINRA 4210 / T6: due close of business the next Fannie Mae business day after receipt (Mon Nov 2 → Tue Nov 3; Tue Nov 10 → Thu Nov 12 across Veterans Day); the confirmed wire under dual control satisfies it.
  o("FINRA_4210_VARIATION_MARGIN_1BD", { trigger: "`margin.call.received`", anchorField: "received_on", offset: "+1 business_days_fannie_et, 17:00 ET", satisfied: "`margin.call.funded`",
    why: "§29.2 timer table: deadline on `margin.call.received`, anchor 'call receipt (ET)', 'the due time in the margin agreement (default: close of business the next `business_days_fannie_et` day — the rule's collection clock)', satisfied by '`margin.call.funded` (wire confirmed)'; breach 'sev 1 → `officer`; dealer may liquidate/close positions after five business days (rule) — liquidity reserve drawn' — FINRA Rule 4210(e)(2)(H): the excess net mark-to-market loss must be margined 'by the close of business on the next business day'; PipelineService.recordMarginCall emits the trigger (marginCallDue) and fundMarginCall emits `margin.call.funded` only under `funding_approver` dual control (dualControlSatisfied)." });
  // Rule 5/6 / T12: the 17:00 ET mark with the GL export by 19:00 ET — the export deadline is the row's due time; re-armed from every completed run.
  o("SM_MTM_DAILY_1700ET", { trigger: "`hedge.policy.updated{initial=true}`", anchorField: "clock_anchor_on", offset: "+1 business_days_fannie_et, 19:00 ET", satisfied: "`mtm.run.completed{gl_exported=true}`",
    why: "§29.2 timer table: recurring, anchor '17:00 ET each `business_days_fannie_et` day (PE–WL live prices close)', '1 `calendar_days`', satisfied by '`mtm.run.completed` and GL export by 19:00 ET'; breach 'sev 2; prior-day marks carried with a flag' — PipelineService.runMarkToMarket emits `mtm.run.completed{gl_exported=true, gl_export_document_id, exported_at, export_hash, clock_anchor_on}` (T12: the export exists by 7:00 p.m. ET and a rerun on the same snapshot and price ids reproduces the hash); the first instance arms with the policy adoption; handleBreach opens the sev 2 officer escalation." });
  o("SM_RATE_SHOCK_REPORT_DAILY", { trigger: "`hedge.policy.updated{initial=true}`", anchorField: "clock_anchor_on", offset: "+1 business_days_fannie_et, 19:00 ET", satisfied: "`rate_shock.report.published`",
    why: "§29.2 timer table: recurring, anchor 'with the 17:00 ET mark', '1 `calendar_days`', satisfied by `rate_shock.report.published`; breach 'sev 3' — the table is published with the mark's GL export (by 19:00 ET); PipelineService.runRateShock emits the event with `clock_anchor_on`, which re-arms the recurring row for the next business day; the first instance arms with the policy adoption." });
  // Rule 8: Fridays 17:30 ET — the policy adoption and every published report carry `liquidity_review_on` (the next Friday), the row's anchor.
  o("SM_LIQUIDITY_STRESS_WEEKLY", { trigger: "`hedge.policy.updated{initial=true}`", anchorField: "liquidity_review_on", offset: "same day", satisfied: "`liquidity.report.published`",
    why: "§29.2 timer table: recurring, anchor 'Fridays 17:30 ET', '7 `calendar_days`', satisfied by '`liquidity.report.published` (margin, pair-off, warehouse and FHFA origination-liquidity coverage)'; breach 'sev 3' — the grammar has no weekday anchor, so `hedge.policy.updated` and every `liquidity.report.published` carry `liquidity_review_on` = the next Friday (ops-29-2.ts nextFridayOnOrAfter) and the row is due that day; PipelineService.computeLiquidityNeed emits the event." });
  // Rule 2 / T9: the 3rd Fannie Mae business day of each month (Mon Oct 5, 2026) — carried as `fallout_review_on` by the policy adoption and by every published review.
  o("SM_BE_FALLOUT_REVIEW_MONTHLY", { trigger: "`hedge.policy.updated{initial=true}`", anchorField: "fallout_review_on", offset: "same day", satisfied: "`fallout.report.published`",
    why: "§29.2 timer table: recurring, anchor '3rd `business_days_fannie_et` day of each month', '1 month', satisfied by 'fallout report and pull-through recalibration proposal published to the partner'; breach 'sev 3' — the grammar has no 'n-th business day', so the anchor is the computed `fallout_review_on` (ops-29-2.ts nthBusinessDayOfMonth / nextFalloutReviewOn) that `hedge.policy.updated` and each `fallout.report.published` carry; PipelineService.analyzeFallout publishes the review (`publishPartnerReport{kind=fallout_review}`) and emits `fallout.report.published{realized_pct, predicted_pct, recalibration_proposed, reasons, consumers=[21.4]}`." });
  // Rule 14: +90 days from the last approved recalibration; the first approved partner version arms it (`initial=true`), later approved versions satisfy and re-arm it; drift beyond ±10 points proposes a version through analyzeFallout.
  o("SM_PULL_THROUGH_MODEL_RECALIBRATION_90", { trigger: "`pull_through.model.recalibrated{initial=true}`", anchorField: "recalibrated_on", satisfied: "`pull_through.model.recalibrated{status=approved}`",
    why: "§29.2 timer table: recurring on `pull_through.model.recalibrated`, anchor 'last recalibration', '+90 `calendar_days` (and on any month with realized pull-through outside ±10 points of prediction)', satisfied by 'new `pull_through_models` version approved by `officer` and 31.2'; breach 'model flagged stale; hedge ratios use the conservative band edge' — PipelineService.recalibratePullThrough refuses without the officer approval and the 31.2 review id and emits `pull_through.model.recalibrated{status=approved, initial, recalibrated_on}`; the first approved version (`initial=true`) arms the row, each later approved version satisfies and re-arms it (kind recurring); handleBreach marks the model stale and moves hedge ratios to the conservative band edge (conservativeTargetFace)." });
  // LL-2026-04 / T13: +1 year from `effective_from`; the first policy version arms it, every later approved version satisfies and re-arms it; while overdue, new locks stay best efforts (hedgeProgramEligibility).
  o("SM_HEDGE_POLICY_REVIEW_1Y", { trigger: "`hedge.policy.updated{initial=true}`", anchorField: "effective_from", satisfied: "`hedge.policy.updated{status=approved}`",
    why: "§29.2 timer table: recurring on `hedge.policy.updated`, anchor `effective_from`, '+1 year `calendar_days`', satisfied by 'new policy version'; breach 'mandatory execution suspended for new locks until reviewed (LL-2026-04 annual-review discipline)' — PipelineService.adoptPolicy emits `hedge.policy.updated{initial, status=approved, effective_from, review_due_on}` (review_due_on = effective_from + 1 year: 2026-10-01 → 2027-10-01); the first version arms the row, every later approved version satisfies and re-arms it; hedgeProgramEligibility excludes new locks from the hedge program (committed best efforts) while the review is overdue (T13); handleBreach opens the officer escalation." });
}

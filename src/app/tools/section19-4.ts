/**
 * §19.4 tools — the spec's tool strings for process 19.4, verbatim, via
 * `defineTools("19.4", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section19.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_19_4: readonly ToolDef[] = [];

/**
 * §30.4 timer overrides (process-owned; applied after every section's so they win the merge — see
 * src/domain/bankruptcy/timers-14-1.ts for the pattern). One `reg.override(code, { trigger?, satisfied | evaluator,
 * anchorField?, offset?, why })` per 30.4 registry row whose trigger/satisfied column names an event the platform
 * spells differently or a condition the column grammar drops; `why` quotes the spec. A code the servicing spec
 * (sections 1–19) owns is never overridden here — reference it. Wired by src/domain/timer-overrides.ts.
 *
 * Every 30.4 row arms only under origination context (src/kernel/timers/engine.ts isOriginationContext): the events
 * ops-30-4.ts appends carry `applicationId` / `application_id` and the scheduler tick carries `source=origination`.
 */
import type { TimerRegistry } from "../../kernel/timers/registry.ts";

export function applySatisfiedOverrides_30_4(reg: TimerRegistry): void {
  const o = reg.override.bind(reg);
  o("SM_TAX_SERVICE_ACTIVATE_2BD", { satisfied: "`vendor.activation.confirmed{vendor_kind=tax_service}`",
    why: "§30.4 timer table: `loan.boarded` (escrowed or not — tax monitoring is life-of-loan) → +2 business_days_servicer; satisfied by '`vendor.activation.confirmed{tax_service}` (contract number, parcel(s) confirmed = 24.4 APN)' — the confirmation event carries `vendor_kind` (data model `vendor_activations.vendor_kind ∈ {tax_service, flood_lol, insurance_tracking, mi_activation}`), so the bare `{tax_service}` selector is the `vendor_kind=tax_service` condition (ops-30-4.ts confirmVendorActivation, which also refuses a contract whose parcels do not match the 24.4 APN)." });
  o("SM_FLOOD_LOL_SERVICING_LINK_2BD", { satisfied: "`vendor.activation.confirmed{vendor_kind=flood_lol}`",
    why: "§30.4 timer table: `loan.boarded` → +2 business_days_servicer; satisfied by '`vendor.activation.confirmed{flood_lol}` (24.5 `flood_determinations.lol_certificate_id` bound to the servicing loan number; 9.6 heartbeat subscription active)' — `vendor_kind=flood_lol` on the confirmation (ops-30-4.ts confirmVendorActivation); breach sev-2 (life-of-loan monitoring is statutory, 42 U.S.C. 4012a(e))." });
  o("REGX_1024_38C2_SERVICING_FILE_5", { trigger: "`servicing_file.requested{kind!=boarding_test}`", satisfied: "`servicing_file.compiled{kind!=boarding_test}`", anchorField: "requested_at",
    why: "§30.4 timer table: trigger '`servicing_file.requested` (any kind other than `boarding_test`)', anchor 'request receipt (`requested_at`, loan TZ)', 5 calendar_days 23:59 (conservative reading of §1024.38(c)(2) \"within five days\"); satisfied by '`servicing_file.compiled` (same request id)' — `requestServicingFile` emits the request with `kind` and `request_id`, `compileHandoffServicingFile` the compile with the same `kind`/`request_id`; the boarding-day test compile (`kind=boarding_test`, SM_SERVICING_FILE_COMPILE_TEST_1BD) neither arms nor closes the statutory clock." });
  o("SM_ORIG_RETENTION_CLASSIFY_GATE", { evaluator: "30.4.retentionClassified", satisfied: "`documents.retention.classified{unclassified_count=0}`",
    why: "§30.4 timer table: not_before_gate on `loan.boarded` — 'every `documents` row indexed for the loan (30.2 `loan_documents_index`) has `retention_class` ≠ null and `retention_anchor_date` set'; satisfied by `documents.retention.classified` — condition-shaped over the indexed documents (evaluators-30-4.ts retentionClassified: facts.documents[].retention_class / retention_anchor_date); `classifyRetention` emits the event with `unclassified_count`, and only a fully classified run (`unclassified_count=0`) clears the gate; breach: `servicing_handoffs.complete` refused (ops-30-4.ts closeHandoff RETENTION_GATE_CLOSED), sev-3 daily until cleared." });
  o("SM_ORIG_EPD_WATCH_P6_60", { evaluator: "30.4.epdWatchOpen", satisfied: "`epd.watch.closed`", anchorField: "first_payment_date",
    why: "§30.4 timer table: recurring window (daily 02:00 loan TZ) from `loan.active` (30.2), anchor '`first_payment_date` → `loans.epd_watch_until` = sixth scheduled due date + 60 calendar_days', offset 'daily inside the window' — a window the offset grammar cannot express (five months plus sixty days); the evaluator asserts the window from `epd_watch_until` (ops-30-4.ts epdWatchUntil; fixture Jan 1, 2027 → Sat Jul 31, 2027) and the daily job (`runEpdDaily`) is the recurring work; satisfied by `epd.watch.closed` at the window end or on payoff/transfer (`closeEpdWatch`); `epd.flag.raised` is the window's output, not a breach." });
  o("SM_FIRST_STATEMENT_RECONCILE_1BD", { satisfied: "`handoff_item.satisfied{item_code=HO-006}`",
    why: "§30.4 timer table: `statement.sent{cycle=1}` (7.1) → +1 business_days_servicer from `sent_at`; satisfied by '`handoff_item.satisfied{HO-006}` after the reconciliation in rule 4' — the item event carries `item_code` (data model `handoff_items.item_code`), so the bare `{HO-006}` selector is `item_code=HO-006`; ops-30-4.ts reconcileFirstStatement emits it only when every figure matches the boarded record (amount due = P&I + escrow, due date, late fee, UPB, escrow balance); a mismatch leaves the clock running → breach sev-2 → 7.1 corrected statement." });
  o("SM_CS_BOARDED_ORIG_REPORT_DAILY", { trigger: "`schedule.tick{cadence=daily, job=cs_boarded_originations_daily}`", anchorField: "date",
    why: "§30.4 timer table: trigger 'schedule 06:00 ET every calendar day', anchor 'report date', offset 'daily'; satisfied by `compliance_report.published{kind=boarded_originations_daily}` — the scheduler's daily tick (ops-30-4.ts dailyReportTick: global subject, `source=origination` so the origination row arms) starts the recurring clock on the report date; `publishDailyReport` closes each day's instance and the engine re-arms the next from the publish; a missing report breaches sev-3 → platform ops." });
}

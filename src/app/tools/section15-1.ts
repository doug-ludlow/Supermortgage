/**
 * §15.1 tools — the spec's tool strings for process 15.1, verbatim, via
 * `defineTools("15.1", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section15.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_15_1: readonly ToolDef[] = [];

/**
 * 32.3 — Entry and the five-minute qualification: the flow-specific UI. The shell renders whatever cards the API creates
 * (src/runtime/borrower/flows/3-entry.ts); this folder carries the one thing a plain card cannot — the thread line
 * authored as a copy reference (the automation disclosure first, every channel).
 */
export { MessageBody, renderMessageBody } from "./MessageBody";

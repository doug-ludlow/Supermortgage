/**
 * §17.4 tools — the spec's tool strings for process 17.4, verbatim, via
 * `defineTools("17.4", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section17.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_17_4: readonly ToolDef[] = [];

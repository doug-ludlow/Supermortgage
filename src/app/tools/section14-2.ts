/**
 * §14.2 tools — the spec's tool strings for process 14.2, verbatim, via
 * `defineTools("14.2", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section14.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_14_2: readonly ToolDef[] = [];

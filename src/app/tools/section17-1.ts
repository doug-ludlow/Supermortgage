/**
 * §17.1 tools — the spec's tool strings for process 17.1, verbatim, via
 * `defineTools("17.1", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section17.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_17_1: readonly ToolDef[] = [];

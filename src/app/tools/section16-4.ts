/**
 * §16.4 tools — the spec's tool strings for process 16.4, verbatim, via
 * `defineTools("16.4", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section16.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_16_4: readonly ToolDef[] = [];

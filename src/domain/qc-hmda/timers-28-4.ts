/**
 * §28.4 timer overrides (process-owned; applied after every section's so they win the merge — see
 * src/domain/bankruptcy/timers-14-1.ts for the pattern). One `reg.override(code, { trigger?, satisfied | evaluator,
 * anchorField?, offset?, why })` per 28.4 registry row whose trigger/satisfied column names an event the platform
 * spells differently or a condition the column grammar drops; `why` quotes the spec. A code the servicing spec
 * (sections 1–19) owns is never overridden here — reference it. Wired by src/domain/timer-overrides.ts.
 * The events named here are appended by src/domain/qc-hmda/ops-28-4.ts (through src/app/tools/section28-4.ts); the
 * hand-off triggers `fraud.suspicious.determined{initial_detection_at, subject_identified}`, `fnma.fraud.reasonable_basis
 * {reasonable_basis_at}`, `ofac.property.blocked{blocked_date}` and `ofac.transaction.rejected{rejection_date}` are also
 * emitted by 22.6 (ops-22-6.ts concludeInvestigation / resolveOfacMatch) with the same field names, and the two
 * production-side referrals (28.1 `qc.fraud_referral.requested`, 28.2 `qc.fraud.referred`, 26.3
 * `funding.fraud_case.requested`) reach the case through ops-28-4.ts signalFromEvent.
 * `FNMA_A3_4_03_FRAUD_SELF_REPORT_30` needs no override: its registry row already parses (anchor `reasonable_basis_at`,
 * +30 calendar_days, satisfied by `fnma.fraud.report.submitted`). `BSA_1029_320_SAR_30` only gains the subject condition
 * (the 30-day clock is the identified-subject track; the 60-day row is the other); the "officer act" of `sar.filed` is
 * enforced where it is emitted (ops-28-4.ts fileSar refuses any actor but a human bsa_officer) — 22.6-T11 records the
 * same event on the return path.
 * `BSA_1029_320C_SAR_RETENTION_5Y` and `OFAC_501_601_RETENTION_10Y` are 31.3's disposal gates (timers-31-3.ts overrides
 * them with its evaluator-backed not-before gates); 28.4's rows are references — this process only emits their anchor
 * events `sar.filed{filed_on, retention_until}`, `ofac.transaction.rejected{rejection_date}`, `ofac.property.blocked
 * {blocked_date}` and `ofac.report.submitted{kind=unblocking}` (the unblocking report sets the blocked record's
 * retention_until — ops-28-4.ts submitOfacReport; the same arithmetic is exposed as sarRetentionGate / ofacRetentionGate).
 */
import type { TimerRegistry } from "../../kernel/timers/registry.ts";

export function applySatisfiedOverrides_28_4(reg: TimerRegistry): void {
  const o = reg.override.bind(reg);
  // Rule 2 / T3: +10 calendar days from `opened_at`; the officer-reviewed triage (either outcome) satisfies; the column's `{suspicious_determined|not_suspicious}` is the `triage_status` field.
  o("SM_FRAUD_TRIAGE_SLA_10", { anchorField: "opened_at", satisfied: "`fraud.case.triaged{triage_status∈{suspicious_determined, not_suspicious}}`",
    why: "§28.4 timer table: 'deadline (internal; makes \"reasonable period\" concrete)', trigger `fraud.case.opened`, anchor `opened_at`, '+10 `calendar_days`', satisfied by '`fraud.case.triaged{suspicious_determined|not_suspicious}` (officer-reviewed)', breach 'sev 1 → `bsa_officer`; `compliance-sentinel` logs the delay as a program metric' — ops-28-4.ts openFraudCase emits fraud.case.opened{opened_at, triage_due_on}; recordTriage emits fraud.case.triaged{triage_status, within_sla, officer_reviewed}; T3: opened Mon Oct 19, 2026 → due Thu Oct 29; no triage → breached sev 1 to the bsa_officer." });
  // Rule 4 / T1: the 30-day clock is the identified-subject track (T2's no-subject case runs the 60-day row instead); anchor = the `initial_detection_at` date the event carries.
  o("BSA_1029_320_SAR_30", { trigger: "`fraud.suspicious.determined{subject_identified=true}`", anchorField: "initial_detection_at",
    why: "§28.4 timer table: 'deadline', trigger `fraud.suspicious.determined`, anchor '`initial_detection_at` date', '+30 `calendar_days`', satisfied by '`sar.filed` (officer act)', breach 'sev 1 → `bsa_officer` + partner `officer`; file immediately; late-filing memo in the SAR file' (31 CFR 1029.320(b)(3)); rule 4: 'if `subject_identified = false`, `due_at = + 60`' — so this row is the identified-subject track — recordTriage (28.4) / concludeInvestigation (22.6) emit fraud.suspicious.determined{initial_detection_at, subject_identified}; fileSar (bsa_officer only — the agent's attempt is refused) emits sar.filed{filed_on, late}; T1: detection Mon Oct 19, 2026 → due Wed Nov 18, 2026." });
  // Rule 4 / T2: the 60-day outer limit arms only when no subject is identified (the column's 'with `subject_identified = false`').
  o("BSA_1029_320_SAR_60_NO_SUBJECT", { trigger: "`fraud.suspicious.determined{subject_identified=false}`", anchorField: "initial_detection_at",
    why: "§28.4 timer table: 'deadline', trigger '`fraud.suspicious.determined` with `subject_identified = false`', anchor '`initial_detection_at` date', '+60 `calendar_days` (the outer limit; the extra 30 days exist only to identify a subject)', satisfied by `sar.filed`, breach 'sev 1; the SAR is filed with \"unknown\" subject fields' (31 CFR 1029.320(b)(3)) — recordTriage (28.4) and concludeInvestigation (22.6) emit fraud.suspicious.determined{initial_detection_at, subject_identified}; fileSar emits sar.filed (bsa_officer only); T2: detection Oct 19, 2026, no subject → due Fri Dec 18, 2026; subject identified Tue Nov 24 → filed by Wed Nov 25 (subjectIdentifiedFileBy) and never after Dec 18." });
  // Rule 4 / T6: the continuing-activity cycle (policy) runs from the prior `filed_on` once the acknowledged SAR's activity continues; a continuing SAR filed or a documented no_file concludes it.
  o("BSA_1029_320_SAR_CONTINUING_120", { trigger: "`sar.acknowledged{activity_continues=true}`", anchorField: "filed_on", satisfied: "`sar.continuing_review.concluded{outcome∈{filed, no_file}}`",
    why: "§28.4 timer table: 'deadline (policy — FinCEN SAR FAQ Oct 9, 2025 …)', trigger '`sar.acknowledged` where activity continues', anchor 'prior `filed_at`', '+120 `calendar_days` (90-day review + filing)', satisfied by 'continuing-activity `sar.filed` or `sar_decisions{no_file}` documented', breach 'sev 1' — acknowledgeSar emits sar.acknowledged{bsa_id, filed_on, activity_continues}; scheduleContinuingReview emits sar.continuing_review.scheduled{review_due_on, continuing_due_on}; fileSar (continuing_activity_of set) and officerSarDecision{no_file} emit sar.continuing_review.concluded{outcome}; T6: filed Wed Nov 18, 2026 → review Tue Feb 16, 2027 → continuing SAR due Thu Mar 18, 2027." });
  // §1029.320(b)(3) last sentence / immediate attention: same-day telephone notice; the determination event carries the flag.
  o("BSA_1029_320_IMMEDIATE_LE_NOTICE", { trigger: "`fraud.suspicious.determined{requires_immediate_attention=true}`", offset: "same day", satisfied: "`law_enforcement.notified`",
    why: "§28.4 timer table: 'gate/deadline', trigger '`fraud.suspicious.determined{requires_immediate_attention}` (e.g., suspected terrorist financing, ongoing wire theft)', anchor 'determination time', 'immediate (policy: same business day)', satisfied by `law_enforcement.notified`, breach 'sev 1' (31 CFR 1029.320(b)(3): \"immediately notify by telephone an appropriate law enforcement authority\") — recordTriage emits fraud.suspicious.determined{requires_immediate_attention}; notifyLawEnforcement emits law_enforcement.notified{agency, method=telephone} (bsa_officer, or the agent with the officer paged)." });
  // Rule 10 / T11: annual (policy) from the scheduler's tick; only a tester other than the compliance officer satisfies it.
  o("BSA_1029_210_INDEPENDENT_TEST", { trigger: "`schedule.tick{cadence=annual, job=bsa_independent_test}`", offset: "annual", satisfied: "`bsa.independent_test.completed{independent_of_officer=true}`",
    why: "§28.4 timer table: 'recurring' on 'schedule', anchor 'policy every 12 months (the rule sets no frequency)', 'annual; `calendar_days`', satisfied by '`bsa.independent_test.completed` by a tester other than the compliance officer', breach 'sev 1 → partner `officer` / SM management' (31 CFR 1029.210(b)(4)) — ops-28-4.ts programScheduleTick appends schedule.tick{cadence=annual, job=bsa_independent_test, source=origination}; recordIndependentTest emits bsa.independent_test.completed{independent_of_officer=true} or bsa.independent_test.rejected{independent_of_officer=false} (T11: the compliance officer's own test leaves the row unsatisfied)." });
  o("BSA_1029_210_TRAINING_ANNUAL", { trigger: "`schedule.tick{cadence=annual, job=bsa_training}`", offset: "annual", satisfied: "`bsa.training.completed{all_appropriate_persons=true}`",
    why: "§28.4 timer table: 'recurring' on 'schedule', anchor 'annual + at onboarding', 'annual', satisfied by 'training records complete for all \"appropriate persons\" (incl. `bsa_officer`, `fnma_portal_operator`, QC, funding staff, developers of the `fraud-risk` agent)', breach 'sev 2' (31 CFR 1029.210(b)(3)) — programScheduleTick{job=bsa_training}; recordTraining emits bsa.training.completed{all_appropriate_persons, missing} (every appropriate person with a completion within 12 months)." });
  o("BSA_1029_210_PROGRAM_REVIEW_ANNUAL", { trigger: "`schedule.tick{cadence=annual, job=bsa_program_review}`", offset: "annual", satisfied: "`bsa.program.reviewed{senior_management_approved=true}`",
    why: "§28.4 timer table: 'recurring' on 'schedule', anchor 'annual (with the risk assessment refresh — NPRM-ready)', 'annual', satisfied by '`bsa.program.reviewed` with senior-management (and board) approval', breach 'sev 2' (31 CFR 1029.210(a); FR 2026-07033) — programScheduleTick{job=bsa_program_review}; reviewProgram (officer act) emits bsa.program.reviewed{senior_management_approved=true, board_approved, risk_assessment_within_12m}." });
  // Rule 6 / T4: the blocked-property report runs from the `blocked_date` the event carries (22.6 resolveOfacMatch and 28.4 dispositionOfacHit both name it); the ORS submission carries `kind`.
  o("OFAC_501_603_BLOCKED_REPORT_10BD", { anchorField: "blocked_date", satisfied: "`ofac.report.submitted{kind=blocked_initial}`",
    why: "§28.4 timer table: 'deadline', trigger `ofac.property.blocked`, anchor 'blocked date', '+10 `business_days_federal`', satisfied by '`ofac.report.submitted{blocked_initial}` via ORS', breach 'sev 1 → `bsa_officer` + partner `officer`' (31 CFR 501.603(b)(1)) — dispositionOfacHit emits ofac.property.blocked{blocked_date, report_due_on}; submitOfacReport (bsa_officer) emits ofac.report.submitted{kind=blocked_initial, ors_reference}; T4: blocked Thu Nov 12, 2026 → due Fri Nov 27, 2026 (Thanksgiving excluded); worked example 2: blocked Nov 13 → due Mon Nov 30." });
  o("OFAC_501_603_UNBLOCKING_REPORT_10BD", { trigger: "`ofac.property.unblocked`", anchorField: "unblocked_on", satisfied: "`ofac.report.submitted{kind=unblocking}`",
    why: "§28.4 timer table: 'deadline', trigger 'property unblocked', anchor 'unblocking date', '+10 `business_days_federal`', satisfied by `ofac.report.submitted{unblocking}`, breach 'sev 1' (31 CFR 501.603(b)(3)) — unblockProperty (bsa_officer) emits ofac.property.unblocked{unblocked_on, report_due_on}; submitOfacReport{kind=unblocking} is the only act that sets the blocked record's retention_until (T4: blocked plus 10 years after the unblocking date)." });
  o("OFAC_501_603_ANNUAL_BLOCKED_0930", { trigger: "`ofac.property.blocked`", anchorField: "blocked_date", offset: "Sept 30", satisfied: "`ofac.report.submitted{kind=annual_blocked}`",
    why: "§28.4 timer table: 'recurring', trigger 'any blocked property held as of June 30', anchor 'Sept 30', 'annual; `calendar_days`', satisfied by `ofac.report.submitted{annual_blocked}`, breach 'sev 1' (31 CFR 501.603(b)(2): \"annually by September 30\" as of June 30) — arms on the block (next Sept 30 on or after the blocked date; re-arms on each annual submission while the property stays blocked; ops-28-4.ts annualBlockedReportDue / annualReportRequired); worked example 2: blocked Nov 13, 2026 → Sept 30, 2027 if still blocked." });
  o("OFAC_501_604_REJECTED_REPORT_10BD", { anchorField: "rejection_date", satisfied: "`ofac.report.submitted{kind=rejected_transaction}`",
    why: "§28.4 timer table: 'deadline', trigger `ofac.transaction.rejected`, anchor 'rejection date', '+10 `business_days_federal`', satisfied by `ofac.report.submitted{rejected_transaction}`, breach 'sev 1' (31 CFR 501.604) — dispositionOfacHit (28.4) and resolveOfacMatch (22.6) emit ofac.transaction.rejected{rejection_date, report_due_on}; T4: rejected Fri Oct 9, 2026 → due Mon Oct 26, 2026 (Columbus Day excluded); record retained to Oct 9, 2036." });
  // Schedules / T10: the daily 05:00 ET SLS refresh + re-screen; a failed refresh leaves the tick unsatisfied (sev 2) and the funding gate closed.
  o("SM_OFAC_LIST_REFRESH_DAILY", { trigger: "`schedule.tick{cadence=daily, job=ofac_list_refresh}`", offset: "daily", satisfied: "`ofac.list.refreshed{rescreen_complete=true}`",
    why: "§28.4 timer table: 'recurring' on 'schedule', anchor 'daily 05:00 ET', 'daily', satisfied by 'SLS lists refreshed and every open application/party re-screened', breach 'sev 2; funding gate stays closed until the day's screen is complete' — programScheduleTick{job=ofac_list_refresh} appends schedule.tick{cadence=daily}; ofacListRefresh emits ofac.list.refreshed{rescreen_complete=true, list_versions} on success or ofac.list.refresh.failed (T10: Thu Nov 12, 2026 failure → SM_OFAC_CLEAR_BEFORE_FUNDING_GATE stays closed for every disbursement that day)." });
  // Gate / T10: evaluator over the disbursement's parties and their screens (≤ 1 calendar day old, none potential/confirmed); arms on 26.3's funding request; the funder cannot bypass it.
  o("SM_OFAC_CLEAR_BEFORE_FUNDING_GATE", { trigger: "`funding.requested`", evaluator: "28.4.ofacClearBeforeFundingGate",
    why: "§28.4 timer table: 'not_before_gate', trigger '`funding.authorized` requested (26.3)', offset 'open iff every party and payee has a screen dated within 1 `calendar_days` with no `potential`/`confirmed_match`', satisfied by '`assertGateOpen` before `disburse` and before any outbound wire', breach 'block' — 26.3 authorizeFunding emits funding.requested before funding.authorized; evaluators-28-4.ts 28.4.ofacClearBeforeFundingGate (ops-28-4.ts ofacClearBeforeFundingGate / assertOfacGateOpen; a `bypass`/`funder_override` fact closes it); worked example 2(b): the payee's potential hit Thu Nov 12, 2026 closes the gate for that payee only." });
  // Red Flags / 16 CFR 681 App. A §VI: annual board report from the scheduler's tick (policy: Q1).
  o("FCRA_681_ITPP_BOARD_REPORT_ANNUAL", { trigger: "`schedule.tick{cadence=annual, job=itpp_board_report}`", offset: "annual", satisfied: "`red_flags.board_report.issued`",
    why: "§28.4 timer table: 'recurring' on 'schedule', anchor 'annual (policy: Q1)', 'annual', satisfied by `red_flags.board_report.issued`, breach 'sev 2' (16 CFR 681.1(e); App. A §VI(b): report to the board \"at least annually\") — programScheduleTick{job=itpp_board_report}; issueBoardReport emits red_flags.board_report.issued{period, elements} (effectiveness, service-provider oversight, significant incidents, recommendations)." });
  // §609(e) / T9: +30 calendar days from receipt of a verified request; records provided without charge or a documented (e)(5) decline.
  o("FCRA_609E_VICTIM_RECORDS_30", { trigger: "`identity_theft.request.received{verified=true}`", anchorField: "received_on", satisfied: "`identity_theft.request.fulfilled{outcome∈{records_provided, declined_e5}}`",
    why: "§28.4 timer table: 'deadline', trigger '`identity_theft.request.received` (verified)', anchor 'receipt date', '+30 `calendar_days`', satisfied by '`identity_theft.request.fulfilled` or a documented (e)(5) decline', breach 'sev 2 → partner `officer`' (15 U.S.C. 1681g(e)(1), (e)(5)) — receiveIdentityTheftRequest emits identity_theft.request.received{verified, received_on, due_on}; fulfilIdentityTheftRequest emits identity_theft.request.fulfilled{outcome=records_provided (without charge) | declined_e5 (declined_reason)}; T9: received Mon Nov 2, 2026 → by Wed Dec 2, 2026." });
  // Rule 4 / worked example 1: +5 calendar days from the draft, capped at the SAR deadline − 3 days — ops-28-4.ts officerDecisionDue computes it and the draft event carries it.
  o("SM_BSA_OFFICER_SAR_DECISION_SLA_5", { anchorField: "officer_decision_due_on", offset: "0", satisfied: "`sar.officer_decision`",
    why: "§28.4 timer table: 'deadline', trigger `sar.drafted`, anchor 'draft time', '+5 `calendar_days` (and never past the SAR deadline − 3 days)', satisfied by `sar.officer_decision`, breach 'sev 1' — draftSar emits sar.drafted{officer_decision_due_on = min(drafted + 5, due_on − 3)}; officerSarDecision (bsa_officer) emits sar.officer_decision{decision, within_sla}; worked example 1: drafted Mon Oct 26, 2026, due Nov 18 → decision by Sat Oct 31; approved Thu Oct 29." });
  // Rule 1 / worked example 1: the production hold overlays an open application (22.6's fraud_hold state that 23.1/25.2/26.x/27.x assert); released on `not_suspicious` or a chosen decision path.
  o("SM_FRAUD_PRODUCTION_HOLD", { trigger: "`fraud.case.opened{production_hold=true}`", satisfied: "`fraud.hold.released`",
    why: "§28.4 timer table: 'gate', trigger '`fraud.case.opened` on an open application', satisfied by '`fraud.hold.released` (`not_suspicious`, or a decision path chosen for a suspicious case)', breach 'blocks `issueCD`, `consummate`, `disburse`, `submitDelivery` (25.2/26.3/29.3 assert)' — openFraudCase emits fraud.case.opened{production_hold=true} on an open application and applyProductionHold reuses 22.6 placeHold (fraud.hold.placed; the `fraud_hold` overlay assertNoFraudHold reads); releaseProductionHold emits fraud.hold.released{path}; assertNoProductionHold covers disburse / submitDelivery." });
}

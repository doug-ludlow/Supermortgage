/**
 * §14.3 tools — the spec's tool strings for process 14.3, verbatim, via
 * `defineTools("14.3", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section14.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_14_3: readonly ToolDef[] = [];

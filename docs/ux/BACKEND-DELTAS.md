# Backend deltas the borrower UX requires

DELTA-01…10 are the ten deltas `14-claude-code-build-plan.md` §3 declares; the reconciliation below is what the import of
this package as spec section 32 (`tools/import_ux.py`, `spec/sections/32-borrower-experience/`) found when every backticked
name in `docs/ux` was checked against `src/`, `db/migrations/` and the spec registries. Nothing here is invented: each row
names the platform object that exists, the UX name that differs, and the default the build takes. Section numbers are the
platform's (Origination O2.3 → 21.3; servicing 1–19 unchanged).

## 1. The ten declared deltas (14 §3)

| ID | Delta | Owner spec touched | State in this tree (2026-09-11) |
|---|---|---|---|
| DELTA-01 | Reg C **preapproval program**: `prequalifications{kind=preapproval, du_casefile_id, approved_amount_cents, valid_until}`; event `preapproval.letter.issued`; 21.6 adverse-action and 28.3 HMDA paths for denied preapproval requests | 20.3 (Q2 overridden), 21.6, 23.1 (TBD casefiles), 28.3 | **Built on this branch (32.3 P8):** `db/migrations/0113_preapproval_program.sql` adds `kind`, `du_casefile_id`, `approved_amount_cents`, `valid_until` to `prequalifications`; `src/domain/leads-pricing/ops-20-3.ts` `issuePreapprovalLetter` / `preapprovalLetterPayload` emit `preapproval.letter.issued{prequal_id, kind=preapproval, du_casefile_id, approved_amount_cents, valid_until, letter_document_id, hmda_preapproval_program=true}` on the application (`requestPrequalification{kind=preapproval}` opens the row); 20.3's `explainProgram{op=issue_preapproval_letter}` renders `NTC_SM_PREAPPROVAL_LETTER` (src/notices/authored/section20-3.ts, registered by the runtime beside the catalog — DELTA: not in spec/registry/notices.json) and writes the documents / prequalifications rows; 23.1's `createCasefile` is property-agnostic (a TBD casefile); 28.3's `createRecord` already takes `preapproval: 1` (the flow's event carries `hmda_preapproval_program`). Still open: 21.6's adverse-action path for a denied preapproval request (no change needed to the notice; the trigger is 23.3's decision). Previously: `db/migrations/0077_lead_intake.sql` created `prequalifications` for the soft-pull prequalification only: columns `prequal_id, lead_id, requested_at, basis ∈ {consumer_stated_only, soft_pull}, soft_pull_report_id, estimated_representative_score, score_source, stated_income_cents, stated_assets_cents, value_estimate_cents, loan_amount_range_cents, ltv_estimate, program_fit, quote_id, outcome, letter_document_id, retention_class, regb_decline_risk_flag`. Missing for the preapproval program: `kind`, `du_casefile_id`, `approved_amount_cents`, `valid_until`; the event `preapproval.letter.issued` is not emitted anywhere in `src/` (`prequal.letter.issued` is). |
| DELTA-02 | **UI-owned tables**: `conversations`, `messages`, `card_instances` (+ `card_instance_events`), `deep_links`, `ui_events`, `sessions` | none (new schema) | **In flight on this branch:** `db/migrations/0111_borrower_ui_tables.sql` creates all seven (plus `auth_challenges`, `passkey_credentials` for L1/passkeys). 32.2's Data model lists the seven as `(new)`; the audit counts them built. |
| DELTA-03 | **Property-data adapter** (`integrations/property-data`): public records, AVM; used by R1/P9/C1 | 20.3, 24.4, 30.3 | **Open** — a `FAKE` adapter in every build stage (README §Vendor fakes). |
| DELTA-04 | **Carrier connection** (`integrations/carrier-connect`), optional path for insurance evidence | 24.5, 9.1 | **Open** — `FAKE` adapter. |
| DELTA-05 | **Standing verification connections**: `consents{kind=blanket_verification_authorization, standing=true}` with refresh/retention policy | 22.3, 22.4, 31.3 | **In flight:** `db/migrations/0112_borrower_consent_kinds.sql` adds the kind and the `standing` column (and `party_id`). The refresh/retention policy (data refreshed only when an opportunity is `offer_ready` and the borrower said Yes) is still to be written in 20.1/31.3 terms. |
| DELTA-06 | **Record projection** `borrower_record`, the servicing history views, SSE stream | api | **In flight:** `src/runtime/borrower` (API seam) and `apps/borrower` (Next.js). |
| DELTA-07 | **Agent tools** `send_card`, `resolve_card_by_evidence`, `create_deep_link` on `borrower-comms` and `intake` | baseline §8 tool allowlists | **Declared:** 32.1's agent paragraph names them; `spec/registry/agents.json` carries them on `intake` (owner of 32.1) — bus registration in `src/app/tools/section32-1.ts` is the build's job (`0/3` in COVERAGE.md). |
| DELTA-08 | **Card-delivered notices**: Notice Registry channel `esign_portal` records `card_instance_id` as delivery evidence | baseline §6 | **In flight:** `src/notices/channel.ts` adds `esign_portal` to `Channel` and `cardInstanceId` to `ChannelDecision`; 0111 adds the channel and evidence columns to `notice_deliveries`. Before this branch the notice channels were `mail_first_class`, `mail_certified`, `email_link`, `portal_post`, `sms_link` only (the disclosures tables of 0064/0065/0070 already knew `esign_portal`). |
| DELTA-09 | **DemographicsCard `collection_method`** value `internet`; `video` treated as not in person | 21.1 rule 3 | **Open** — check the `applicant_demographics.collection_method` domain before the card writes it. |
| DELTA-10 | **Per-listing estimate** for preapproved borrowers under an approved quote id without a new MLO review while `SM_QUOTE_VALIDITY_GATE` is open | 20.4, 20.3 | **Open.** |

Open legal positions the UX carries as flags, not deltas: `origination.ai_mlo_intake` (20.3 Q3), `live_contact.ai_voice_counts` (11.1),
the same-creditor rescission exemption (25.3), Reg C preapproval adoption (DELTA-01).

## 2. Names the UX spells differently from the platform (reconciled by grep of every backticked dotted name in docs/ux against src/)

The UI maps at the SSE / command boundary; the spec files of section 32 keep the UX text and note the platform spelling in
their "Discrepancies vs blueprint" line. Default: **the platform spelling wins**; nothing is renamed in 1–31.

| UX name | Platform name (src) | Where the UX uses it | Note |
|---|---|---|---|
| `payments.reversed` | `payment.reversed` | 08a §3.4 (32.8), 02 §3 | 2.x return handling; the projection's `payments_view.status = reversed` is a column value, not an event. |
| `party.identity.verified` | `identity.verified` | 03 E5 (32.3) | 22.6 emits `identity.verified`; the UX's "O3.6 naming" is not the platform's. |
| `human.transfer.completed` | `human_transferred` | 01 §3.17, 02 §3, 11 SQ-30, 13 T-X-08 (32.1, 32.2, 32.13) | `human.transfer.requested` exists as spelled; only the completion event differs. |
| `autodraft.change.requested` | the `autodraft.enrollment.*` family (`autodraft.enrollment.requested → authorized → validating → active`, changes re-enter at `requested`) | 01 §3.12, 02 §2 (32.1, 32.2) | 2.x has no separate change event; a change is a new enrollment version. |
| `signing_sessions.consent_captured` | column state `signing_sessions.status = consent_captured` | 02 §2 `closing.captureEsignConsent` | Listed in the "Events emitted" column but it is a state, not an event (26.2). |
| `preapproval.letter.issued` | none — `prequal.letter.issued` exists | 02 §3, 03 P8 | DELTA-01. |
| `disclosure.*.received` (wildcard) | `disclosure.le.received`, `disclosure.cd.received`, `disclosure.companion.received` | 02 §1.3 | Each concrete name exists. |

Every other event name in 02 §2/§3 and in the **Events** bullets of 03–10 (`lead.disclosure.delivered`, `consent.ai_disclosure.acknowledged`,
`credit.report.received`, `terms.presentation.requested`, `mlo.review.completed`, `application.trid_received`, `disclosure.le.*`,
`intent.to_proceed.received`, `lock.executed`, `condition.*`, `decision.issued`, `valuation.*`, `flood.*`, `mi.*`, `closing.*`,
`rescission.*`, `funding.*`, `loan.funded`, `loan.boarded`, `loan.purchased`, `payment.posted`, `ach.return.received`,
`escrow.analysis.completed`, `loan.delinquency.day_reached`, `lossmit.*`, `refi.opportunity.*`, `contact.qrpc.achieved`,
`communication.inbound.received`, `human.transfer.requested`, `continuity.assigned`, `escalation.opened`, `fee.waived`,
`rov.requested`, `recording.*`, `marketing.response.received`, `changed_circumstance.recorded`) is spelled as the platform spells it.
The UX commands of 02 §2 (`lead.start` … `party.updateContact`, 45 with the four `autodraft.*` verbs) and the copy keys of 12 are
new by design — they are the `borrower-app` agent's tools in 32.2 and the copy library, not platform events.

## 3. Consent kinds and E-SIGN scope classes

`consent_kind` (0001) was `esign, tcpa_voice, tcpa_sms, email_marketing, autopay, ai_voice, language_preference`, extended by 0060
(`escrow_credit_to_new_loan`), 0077 (`ai_disclosure_ack`), 0089 (`esign_disclosures`). The ConsentCard (01 §3.5) captures
`esign, credit_authorization, tcpa_voice, tcpa_sms, ai_disclosure_ack, irs_estatement, autodraft_authorization, joint_intent,
blanket_verification_authorization`:

| UX kind | Platform | State |
|---|---|---|
| `credit_authorization` | missing before this branch | added by 0112 |
| `joint_intent` | missing | added by 0112 |
| `irs_estatement` | missing (7.4's `tax_statements` class was folded into `esign` scope) | added by 0112 |
| `blanket_verification_authorization` (+ `standing`) | missing | added by 0112 (kind + `standing` column) |
| `autodraft_authorization` | exists as `autopay` | UX maps to `autopay`; default: keep `autopay` |
| `tcpa_voice` / `tcpa_sms` with `purpose ∈ {informational, marketing}` | kinds exist; `purpose` lives on the 0076 solicitation consent rows | map at the command handler |
| E-SIGN scope classes `origination_disclosures`, `origination_esign_signatures`, `enote`, "servicing classes" | `consents.scope text[]` (0009) with the class vocabulary documented on the column by 0112: servicing — `periodic_statements, escrow_statements, regx_correspondence, arm_notices, privacy_notices, lossmit_notices, early_intervention_notices, insurance_notices, pmi_notices, payoff_statements, general_correspondence`; origination — `disclosures, notices`; closing — `esign_signatures, enote` | the UX class names map to the column vocabulary (`origination_disclosures` → `disclosures`, `origination_esign_signatures` → `esign_signatures`); default: the column's names |

## 4. Timers and notices the UX names that are not registry codes

| Name | UX use | Registry | Default |
|---|---|---|---|
| `FNMA_NIB_BALANCE_NOTICE` | 10 §1.2: "if a deferred non-interest-bearing balance was included" at `paid_in_full` (32.12) | not a timer code; 16.2's non-interest-bearing balance advice is a notice, not a clock | render the 16.2 notice; no new timer |
| `SM_QC_PREFUNDING_HOLD` | 06 §7 and T-06-12: "a final review is in progress" (32.6) | not a timer code; the pre-funding QC hold is a 28.1 state (`FNMA_D1_2_01_PREFUNDING_PRIOR_TO_CLOSING_GATE` is the registry gate) | render from the 28.1 hold state; the T-id text keeps the UX name |
| `NTC_STATE_ANNUAL_ESCROW_STMT_UT` | 08a §6.2 state escrow statements (32.8) | not in `spec/registry/notices.json` (3.3 names the state escrow statements under other codes; `NTC_UT_7_17_4_RESERVE_OPTIONS` is the Utah code the registry has) | listed without backticks in 32.2 and 32.8 so no 32.x process becomes an owner; author under 3.3 if a Utah annual statement is required |

The 42 timer codes of 02 §4 (the borrower-visible allow-list) all exist; 32.2's "Timers and gates" table carries them as bare
reference rows (Kind `(N.M owns)`, empty trigger/anchor/offset/satisfied cells, label in Breach). The registry keeps the earlier
owner; the audit counts each row as a 32.2 unit that is built exactly when the owning definition is armable, satisfiable and emitted.

## 5. Ownership rules the import applied

- No `NTC_` code is owned by a 32.x process: `spec/registry/notices.json` lists 319 codes after the import, none with a 32.x
  `owner_process`; the 32.x files only mention codes an earlier section names.
- No timer is owned by a 32.x process: 42 reference rows in 32.2, none defined.
- Tables: the seven UI-owned tables are 32.2's; every other table the UX reads is named in prose as a read-only projection source.
- Tools: 32.1 → `send_card`, `resolve_card_by_evidence`, `create_deep_link` (agent `intake`, with `borrower-comms`); 32.2 → the 45
  UX commands (agent `borrower-app`, added to `tools/extract_agents.py` KNOWN).
- Figures: none — the UX quotes fixture amounts ($560,000; $8,200/month) without cents and bolds no money figure.

## 6. Deltas the 32.6 build implemented (decision, property, insurance, MI, clear to close)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| A command's `EscalationService` is seeded with the scope's still-open escalations (`PgEscalationRepository.openFor`, `EscalationService.seed`, `Runtime.executeDef`) | kernel (`src/app/escalations.ts`, `src/runtime/app.ts`), no schema change | `openReviewerEscalation{op: decide}` can complete the `underwriting_reviewer` escalation `recommendDisposition` opened in an earlier unit of work; before this, every command started with an empty service and 21.6's reviewer step failed with `no escalation <id>` under the runtime (it only worked in 21.6's single-service harness) | 32.6-T2 / T3: the counteroffer and adverse-action notices issue only after the reviewer's decision |
| `disclosure.acknowledgeReceipt{kind: flood_notice}` delegates to 24.5 `deliverNotice{op: acknowledge}` | 32.2 (`src/app/tools/section32-2.ts`), additive branch | the borrower's acknowledgment of `NTC_FDPA_4104A_FLOOD_NOTICE` from the DocumentCard becomes 24.5's own `flood.notice.acknowledged` | 32.6-T9 |
| `borrower_record.read_only` and `property.{valuation,flood,hazard,project_review}.label` | 32.2 projection (`src/runtime/borrower/record.ts`, `serialize.ts`), additive keys | a terminal disposition (denied, withdrawn, closed) leaves the Record read-only; the Property rows carry their 01 §4 labels ("No appraisal needed", "Insurance — one thing to fix", "Waiting on HOA documents") | 32.6-T3, T4, T7, T8 |
| `entity_records` keyed per scope — `db/migrations/0115_entity_records_scope.sql` adds `scope_key` (generated: `coalesce(application_id, loan_id, '')`) and re-keys the primary key as (kind, id, version, scope_key) | kernel storage, additive migration; `PgEntityRepository` unchanged | the tools number rows over the command's scoped store (24.1 `sel-n`, 24.5 `fd-n`/`ir-n`/`ev-n`, 28.1's plan-derived ids), so two applications legitimately hold the same (kind, id, version); before this the second application's commit failed with a unique violation (409 `DUPLICATE_RECORD` on the borrower routes, 500 on the tool routes — `src/runtime/borrower/errors.ts` documents the symptom) | 32.6-T4 (24.1 `selectMethod`), T8/T9 (24.5), T12 (28.1) on any database that already holds another application's rows |
| `mi_quotes` rows carry `loan_amount_cents` (24.6 `requestMiQuotes` stores the loan amount each insurer priced) | 24.6 (`src/app/tools/section24-6.ts`), additive field on the stored row | `mi.selectPlan` → 24.6 `recordPlanElection` needs the loan amount the quote was priced on; neither `mi.quote.received` nor the quote row carried it, so the borrower flow had no platform figure to hand 24.6 | 32.6-T10 |
| DU resubmission on a borrower-changed loan amount | none — the 32.6 flow calls 23.1 `evaluateResubmission` as `underwriter` on `application.six_item.captured{item: loan_amount_sought, source: borrower}` after the low-value "cash" lever | 23.1 has no listener of its own for a post-decision six-item change; the flow supplies the baseline (the last `du_submissions` findings) and the candidate snapshot | 32.6-T6 |

## 7. Deltas the 32.5 build implemented (verification, needs list, conditions, second borrower)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| The condition lifecycle step `open` → `waiting_borrower` (needs-list item sent) / `waiting_third_party` (vendor order) | 23.3 (`src/domain/underwriting/ops-23-3.ts` `markConditionWaiting`, additive; `src/app/tools/section23-3.ts` `reopenCondition{op: "waiting", on}`), event `condition.waiting{status, on, prior_status}` | The 23.3 spec names the step ("Condition lifecycle: `open` → `waiting_borrower` (needs-list item sent) or `waiting_third_party` (vendor order)") but no code path transitioned it; 23.2 opened conditions `open` and 23.3 only cleared/waived/reopened them. | 32.5 §1: only `waiting_borrower` items are the borrower's; the 32.5 flow asks 23.3's own tool as `underwriter` when it puts the item in front of the borrower (T1, T2) — the UI never transitions the owning process's state. |
| One initial LE issued per consumer on the channel each consumer's E-SIGN state allows | 21.2 (`LoanEstimateService.deliverPerBorrower`, `LeRow.deliveries`, additive), the runtime bridge (`src/runtime/origination.ts` `LoanEstimateDeliveryInput.delivery.deliveries[]`), the route (`POST /v1/applications/{id}/disclosures/le` passes `delivery.deliveries` through) | `deliver()` issued the one LE on one channel; 7.4 rule 4 (consent is per party) and 32.5 §7 need the LE electronic to the consenting party and mailed (with the vendor's proof) to the other. The spec's channel events carry `borrower_id`; ONE `disclosure.le.issued{per_borrower, completed_by_borrower_id, completed_by_channel}` completes the issuance and satisfies `REGZ_1026_19E1_LE_3BD`; the mailed delivery governs the mailbox presumption and the 7-SBD gate. The 32.4 flow's LE cards honour `borrower_id` (`partiesFor`, behaviour-preserving when absent). | 32.5 T9: the DocumentCard to A only, the mailed StatusCard to B, the Record's per-party delivery status, the timer satisfied by the mailing to B. |
| `credit.authorize` refused by `SM_O21_JOINT_INTENT_GATE` until the invitee affirms | 32.2 (`src/app/tools/section32-2.ts` guardrail on `credit.authorize`; `application.confirmField{path: credit.alert.<id>}` → 22.2 `triageUdmAlert` + 23.1 `evaluateResubmission`; `application.inviteParty` accepts the InviteCard's `party_role` and composes the legal name from `contact.first_name/last_name`; `document.upload` carries `request_id` to 22.1) and the API (`src/runtime/borrower/commands.ts` `jointIntentFacts`: required only while 21.1's gate is armed on the application, affirmed from the intake record) | 21.1 rule 4's gate was evaluated only inside 22.2's `orderCreditReport` (`joint_intent_facts` input); the borrower's own `credit.authorize` command had no fact to check. | 32.5 T7: the invitee's `credit.authorize` answers `{code: SM_O21_JOINT_INTENT_GATE, gate, copy_key: gate.joint_intent.each_borrower}` until their own ConsentCard{joint_intent} is affirmed; T5: the ConfirmCard's yes adds the liability (22.2 `verified_new_debt`) and runs 23.1's B3-2-10 test (`du.resubmission.required`). |
| `send_card{at?}` — a card dated to the fact it answers | 32.1 (`src/app/tools/section32-1.ts` `send_card`, optional `at` input, never later than `ctx.now`); the flow dates its asks to the reacted event (`document_request.opened.requested_at`, the batch's latest `occurredAt`) and passes the same `at` to the owning tools it asks (22.1 `openRequest{at}`, 23.3 `reopenCondition{op: "waiting", at}`, 21.1 `captureField{at}`) | Flows react after commit; a card created from the clock's later reading could not be shown to sit inside the owning process's clock (`SM_DU_CONDITIONS_SLA_4H`) when the reaction is queued behind other work. | 32.5 T1: the UploadCard exists within `SM_DU_CONDITIONS_SLA_4H`; the request's `requested_at`/`due_at` are the condition's instant + 5 calendar days (22.1's own schedule). |
| `borrower_record.needed_from_you[].{owner, label_copy_key, copy_tokens}`, `what_we_are_doing[]`, `needed_summary`, `people[].waiting`, `documents[].deliveries[]` | 32.2 projection (`src/runtime/borrower/record.ts`, `serialize.ts`), additive fields | 02 §1.1 names `needed_from_you` and `people` but not the owner split, the strip count, the invitee's waiting state or a per-consumer delivery status. | 32.5 §1 (one list, one owner per item; the nothing-needed state — T1, T11), §7 ("{{first name}} — invited, waiting"; per-party delivery status — T7, T9). |

## 6. 32.13 cross-cutting (the harness): what the acceptance tests needed from 1–31 and the seam

| What | Where | Note |
|---|---|---|
| `human.transfer.completed` is emitted | 20.3 `deliverDisclosure{op: human_joined, lead_id, interaction_id, human_agent_id, human_agent_name?, escalation_id?}` (`ops-20-3.ts completeHumanTransfer`) — additive op; the interaction records `human_agent_id` and `ai=false`; a human caller's own role completes the escalation when it was opened in the scope | T-X-08 / SQ-30 ("after `human.transfer.completed`, a `PersonCard{human_agent}` exists"). §2 keeps the platform spelling `human_transferred` for 11.3's `contact.logged{outcome}`; the 32.13 flow treats `human.transfer.completed`, `contact.logged{outcome=human_transferred}` and `escalation.completed{kind=human_agent}` alike. |
| `consents` row follows `consent.esign.active` | `flows/13-cross-cutting.ts`: `UPDATE consents SET status='active', verified=true WHERE id=consent_id AND kind='esign'` on `consent.esign.active` / `consent.granted{kind=esign,status=active}` | T-X-04: 20.3's demonstration path (`captureConsent{op: demonstrate}`) emits the event on the lead without touching the 0009 table; 32.2's `consent.capture{op: verify}` already writes it. No 7.4 change. |
| Read-only after terminal | `commands.ts` refuses every command but `case.open`, `human.request`, `party.updateContact` with `SUBJECT_TERMINAL` (copy `error.read_only`) once `terminalStateOf` reads `decision.issued{denial}` / `application.withdrawn` / `application.closed_incomplete` / `rescission.exercised` / `loans.status ∈ {paid_off, transferred_out}` / `loan.paid_in_full` | T-X-16. Gap: nothing in 1–31 sets `loans.status = transferred_out` (17.1's cutover is batch-level: `transfer.batch.cutover_completed`); the harness sets the column directly and says so. The owning fix belongs to 17.x. |
| Degraded vendor | `routes.ts` Truv webhook `voie.report.failed` → `connectorFailed` (32.13): evidence `{outcome: failed}` without any vendor code, thread line `connect.failed.fallback`, UploadCard `income.upload.fallback` | T-X-12. FAKE Truv only; no vendor change. |
| `GET /v1/borrower/me` carries `partner {legal_name, nmlsr_id}` | `routes.ts partnerFor` (the application's intake record / the loan's partner party); `serialize.ts` `me` shape | The shell's automation marker and `{{partner.legal_name}}` in the disclosure line (01 §7.1). |
| DemographicsCard prompt lists on the wire | `serialize.ts opaque`: a card's `ethnicity` / `race` / `sex` option lists (`[{id, label}]`) are sent as `ethnicity_options` / `race_options` / `sex_options`; `DemographicsCard.tsx` reads those first | T-X-03 keeps the answer-shaped names off every response; without the rename the scrub emptied the card and it could not render (T-X-10). Answers still travel only in the resolve request (`answers`), never on a card or a ui_event. |
| Shell ↔ API adapter | `apps/borrower/lib/api/adapt.ts` (`toMe`, `toRecord`, `toThread`, `subjectId`) | The API sends 02 §1.1 verbatim; the shell adds `header`, `timezone` and the numbers `phase` — never a date, amount or rate. |

## 8. Deltas the 32.7 build implemented (CD, closing, rescission, funding, boarding)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| `disclosures{kind=cd|corrected_cd}.figures` — the rendered CD's figure snapshot (`rate_pct`, `apr_pct`, `pi_cents`, `loan_amount_cents`, `cash_to_close_cents`, `lender_credits_cents`, `payoffs_and_payments_cents`, `monthly_escrow_cents`, `initial_escrow_payment_cents`, `fees[]`) and `supersedes` on the corrected row | 25.2 (`src/app/tools/section25-2.ts` `cdFigureSnapshot`, stored by `renderCd` and `scheduleCorrectedCd`), additive fields on the stored row; no schema change | The CD DocumentCard's What-changed block is the LE→CD row diff of two figure snapshots (21.2's `disclosure.le.rendered` payload, 25.2's row), never free text; a corrected CD diffs against the prior version's snapshot (32.7 §1, T1, T2). |
| Document class `fnma_loan_purchase_letter` (family `other`, no freshness rule, not a credit document) | 22.1 (`src/domain/verification/ops-22-1.ts` catalogue; `db/migrations/0114_fnma_letter_document_class.sql`, append-only) | The borrower forwards Fannie Mae's letter through the UploadCard; 22.1 classifies it (borrower-declared, FAKE pass) and 30.4 `explainFnmaLetter{evidence_document_id}` / 25.4 `evaluateOwnershipTransfer{op: borrower_report}` evidence `ownership_transfer_notices` from it (32.7 §6, T13). |
| `card_instances.props` merged in place — `PgBorrowerUiRepository.mergeCardProps` | 32.1 UI tables (`src/infra/db/borrower-ui.ts`), additive method; status and its events untouched | 32.6 sends the closing ScheduleCard on `disclosure.cd.waiting_period.computed` (its T9/T11 count exactly one per party); 32.7 owns the closing-type decision (26.2 `decideClosingType`: RON only when `SM_O72_RON_STATE_AUTH_GATE` is open, else ipen · hybrid · wet) and adds `closing_type_options`, typed `slots`, `command_args{…closing_type_preference}` to that one card instead of sending a second ask (32.7 §2, T4, T5). |
| `borrower_record.dates[]` rows for the condition-shaped gates `REGZ_1026_19F1_CD_3SBD_GATE` (25.2's `earliest_consummation_date`) and `REGZ_1026_23_RESCISSION_3SBD_GATE` (25.3's `expires_at`); badge **Cancelled** (`rescission_periods.status = rescinded`, read-only, `needed_from_you = []`); a CD version listed `superseded` once `disclosure.cd.corrected{supersedes}` names it; `mailed_at` on a CD whose consumer copy 25.2 delivered by mail/courier | 32.2 projection (`src/runtime/borrower/record.ts`), additive | Timers with `due_at = null` never reach the allow-listed Dates rows; the dates come from the owning processes' own events (T1, T2, T3, T6, T8). |
| Journey fixture: `closingDisclosure({ deliveries: false })`, `openFunding()` / `disburse()` / `disburseFromAdvance()`, `deliverAndPurchase()`, `openServicingHandoff()`, `closeAndSign({ snapshot })` | `src/runtime/borrower/fixtures/journey.ts`, additive methods and options | The 32.7 tests deliver the CD per consumer themselves (channel, receipts, the mailbox rule), resume funding after a hold (T9 → T10), reach `loan.purchased` through 29.4/30.1 and open 30.4's hand-off so HO-009 is evidenced (T13). |
| UI: `DocumentCardProps.how_to_cancel` (quiet link → a borrower message, never a primary button), `WhatChanged.title_key`, `ScheduleCardProps.closing_type_options` + `ScheduleSlot.closing_type`, `ConsentCardProps.elements` + `optional_statement_copy_key`, `StatusCardProps.detail_copy_key(s)`, `HandoffCardProps.*_copy_key` + `explainer_points`, `StatusBadge "Cancelled"`, `CardComponentProps.onMessage` (Shell wires `sendMessage`) | `apps/borrower/lib/types/cards.ts`, `lib/types/record.ts`, `components/cards/*`, `components/shell/Shell.tsx`, `components/flows/7-closing/` — additive | 32.7 §1–§6: every sentence stays a copy key; the closing type is chosen inside the one ScheduleCard; the H-8 card's cancel path is a quiet link; the autodraft authorization shows every 2.3 element and the Reg E optional statement. |
| `closing.selectSlot` passes the slot's `closing_type_preference` to 26.2 as `borrower_election` as well | 32.2 (`src/app/tools/section32-2.ts`), additive input key on the delegate | 26.2's `decideClosingType` reads `borrower_election` (A2-4.1-03: "wet" when the borrower says paper); the UX name alone never reached the decision, so a paper election could not commit `closing_type = wet` (32.7 §2, T5). |

Observed, not changed: 26.3's `RescissionRefused` (the `REGZ_1026_23_RESCISSION_3SBD_GATE` refusal on `requestWarehouseAdvance`) reaches the tools route as an unhandled error (HTTP 500 `{"error":"internal"}`) rather than a named refusal; 32.7-T6 asserts the refusal on the bus (`Runtime.execute`) where it keeps its name.

## 8. Deltas the 32.9 build implemented (servicing: insurance, PMI, ARM, life events, requests)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| `Runtime.executeDef(def, req)` — a tool definition executed on the runtime's unit of work without being in `ALL_TOOLS` | kernel runtime (`src/runtime/app.ts`), additive; `execute` now delegates to it | the 4.x case commands (`SECTION_04_CASE_COMMANDS`: `case.noe.open`, `complaint.*`, `sii.*`) are not on the bus registry; the 32.9 flow runs them as the `case` agent with the same persistence, timers and post-commit feed as any bus tool | 32.9-T7, T8, T11 |
| 4.1 `case.notice.send{case_id, template, recipients, payload}` — the case letters through the Notice Registry, `case.notice.sent` beside `notice.sent` | 4.1 (`src/app/tools/section04.ts`), additive command; template allow-list `CASE_NOTICE_TEMPLATES` | the NoE / complaint acknowledgments and the SII document list are rendered and sent by the case agent, never composed by the surface | 32.9-T7, T8, T11 |
| 4.4 `sii.potential_successor.identify` also writes `loan_parties{role=potential_successor}` (deferred to the command's transaction) | 4.4 (`section04.ts`), additive `deferWrite` | 02 §6: the reporter becomes a `potential_successor` on the loan — correspondence only, no subject, no account information before confirmation | 32.9-T7 |
| 10.1 `pmi.*{op: withdraw}` (`withdrawCancellationRequest` → `mi.cancel.withdrawn{fee_refund_cents, valuation_ordered}` + `mi.valuation_fee.refunded`) and `pmi.*{op: fee_wait_expired}` (`expireFeeWait` → `mi.case.expired{timer: SM_MI_FEE_WAIT_60}`); `evaluate` / `smdu.*{valuation.order}` advance the open `pmi_cancel` case (`awaiting_fee`, `valuation_ordered`, …) | 10.1 (`src/domain/pmi/ops-10-1.ts`, `src/app/tools/section10.ts`), additive ops | the spec's `awaiting_fee` expiry at 60 days and the refund on withdrawal before an order are 10.1 state-machine moves the borrower's taps and the sweep's breach trigger; the flow only calls them | 32.9-T5 |
| 9.6 `sendMapChangeNotice` → `INS_FLOOD_MAP_CHANGE_NOTICE` through the registry, then `flood.map_change.notified{coverage_required, required_cents, borrower_deadline, …}` | 9.6 (`src/domain/insurance/ops-9-6.ts`), additive op and event | the map-change notice named by 9.6's outputs had no code path; the borrower's NoticeCard hangs on its event | 32.9-T3 |
| 32.2 `insurance.submitEvidence` servicing branch (`servicing` / `fpi_case_id` on a loan): `insurance.evidence.received{fpi_active=true}` on the loan then 9.5 `evaluateEvidence`; `pmi.requestCancellation{withdraw}` → 10.1 withdraw; the request's 10.1 channel is `portal` (`verbal` for a spoken turn) and the API-added facts never reach the owning tool's input | 32.2 (`src/app/tools/section32-2.ts`), additive branches | the UploadCard on an FPI case starts REGX_1024_37G_FPI_CANCEL_REFUND_15 at receipt; the withdrawal ChoiceCard reaches 10.1 | 32.9-T2, T5 |
| 32.2 projection: `registerFlowTimers` (a flow's allow-listed clocks beside the 02 §4 rows), `numbers.arm_estimate` (from `arm.initial_notice.sent`) — `record.ts`, `serialize.ts` | 32.2 (`src/runtime/borrower/record.ts`, `serialize.ts`), additive | the FPI, flood, PMI, case and ARM clocks render in Dates with their labels; Numbers shows the engine's estimated payment | 32.9-T3, T6 |
| `InboundMessage.claimed_subject` (the subject the request body named even when it is not one of the party's own) | 32.2 API (`src/runtime/borrower/flows/index.ts`, `commands.ts`), additive optional field | a potential successor names the loan they are writing about before they have any subject (4.4) | 32.9-T7 |

## 9. Deltas the 32.12 build implemented (exits: payoff, transfer out, successor, closed)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| Notice Registry memory across commands — `NoticeServiceDeps.notices?: Map` and `Runtime.noticeMemory` (one map for the life of the runtime, passed to every per-command `NoticeService`) | 6.x Notice Registry (`src/notices/service.ts`), runtime (`src/runtime/app.ts`), additive optional dep | A notice rendered by one unit of work is readable by the next: 17.2's `runContentChecklist` over a notice an earlier command rendered; the 32.12 flow puts the template's own rendered text on the NoticeCard (`plain_language`) instead of a summary. In-memory beside the `notices` table; the event log stays the record. |
| 17.1 `buildTransferPlan{op=milestone, to=approved}` carries `notice_mode` (separate \| combined, input) and `respa_effective_date` (input, else `respaEffectiveDate(transfer_date, installments_due_on_1st)`) on `transfer.batch.approved` and on the stored batch | 17.1 (`src/app/tools/section17-1.ts`), additive payload fields | The registry keys `REGX_1024_33B3_COMBINED_15` on `transfer.batch.approved{notice_mode=combined}` with anchor `respa_effective_date` and `REGX_1024_33B3_GOODBYE_15` on the same anchor; the transfer-out approval event lacked both, so neither clock could arm on a 17.1 batch. |
| 17.2 `ingestMailReturns{op=proofs}` — the print vendor's proofs of mailing for a goodbye / combined / corrective run through inbound.ts `noticeRunMailed` (one `notice.mailed` per loan, the run-level `notice.mailed{every_loan=true}` on the batch); the run row (`transfer_notice_runs`) keeps the proofs, `status=complete`, and the `goodbyeTiming` facts (`transferor_stops`, `transferee_starts`, `window_end`, `ach_cancel_by`, `short_year_due`) | 17.2 (`src/app/tools/section17-2.ts`), additive op | The satisfaction event of the −15 clocks existed only in 1.3's in-memory service and 17.2's vendor path; the Record renders the transfer dates from the stored run row (never a computed date) and the Loan section's autopay end date from `ach_cancel_by`. |
| 17.3 `runOutboundDqGate{op=receipt}` with the RESPA facts on the input (`respa_effective_date`, `due_date`, `grace_days`, `instrument`, `transferee{name, remittance_address, tollfree}`) classifies the receipt by 17.2's §1024.33(c) rule — `payment.received{loan.status=transferred_out, protected}` + `payment.misdirected.received{protected}`, then (forwarding being the default) `misdirected_payment.forwarded` + `payment.misdirected.forwarded`; `protected` written on the `misdirected_payments` row | 17.3 (`src/app/tools/section17-3.ts`), additive branch | 32.12 §2 / T6: the Thread says a post-transfer receipt is forwarded and protected inside the 60 days and not from day 61; the row's `protected` is what the late-charge and delinquency runs read. Without the facts the op behaves as before (`protected: false` row only). |
| 4.4 `sii.determine{confirmed, party_id}` writes `loan_parties{role=confirmed_successor}` (deferred to the command's transaction) when the party is a platform party (uuid) | 4.4 (`src/app/tools/section04.ts`), additive `deferWrite` | The confirmed successor's own sign-in sees the loan (02 §6 scoped roles, `borrower-parties.ts SCOPED_LOAN_PARTY_ROLES`) and can open their own RFI (32.12 §3 / T7). |
| 32.2 projection: `exitsBadge` / `exitsDates` / `exitsLoanFields` / `exitsDocumentsFor` (`src/runtime/borrower/flows/12-exits-record.ts`) — badges Paying off · Paid off · Closed · Servicing moving · Transferred out with `one_liner_tokens`; Dates rows from the stored goodbye-run facts and the batch-subject clocks; `loan.autodraft.ends_on / terminated_on / termination_reason`, `loan.ratewatch_status=void`; a declined successor sees only the notices addressed to them; offers hidden after payoff / transfer; notice prefixes `NTC_PAYOFF_`, `NTC_LIEN_RELEASE_`, `NTC_REGX_36C_`, `NTC_REGX_36D_`, `NTC_REGX_1024_33C_`, `NTC_ENOTE_`, `NTC_NOTE_RETURNED`, `NTC_IRS_1098` visible; "Transferred out" read-only | 32.2 (`src/runtime/borrower/record.ts`, `serialize.ts`), additive | 32.12 §1.2, §2, §3 Record columns. |
| Journey-driven facts the spec text states differently (recorded, not changed): (a) T3 "escrow refund scheduled by Mar 23" — the registry's `REGX_1024_34B_PAYOFF_ESCROW_REFUND_20BD` is 20 `business_days_federal` from the Mar 3 posting date (Mar 31), which is what the engine arms and 16.2's `escrow_refund` task carries; the test asserts the engine's date. (b) "rate-watch is `void`" — 20.1 has no `void` status (`OpportunityStatus`); the Record reports `loan.ratewatch_status=void` and hides offers once `loan.paid_in_full` / a transfer out is on the spine, and the flow files `payoff.ratewatch_ended`. (c) T6's `REGX_1024_33C1_LATE_FEE_PROTECTION_60` triggers on `transfer.batch.cutover_completed` (17.1's cutover milestone, not driven here); the protection classification and the window end come from 17.2's rule and the stored run row. | — | — |

## 10. Deltas the 32.10 build implemented (servicing: hardship and delinquency)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| 11.1 counter job on the bus: `delinquencyCounterJob(input)` (pure) + `src/runtime/delinquency.ts` `delinquencyDailySweep(rt, nowIso)` | 11.1 (`src/domain/early-intervention/ops-11-1.ts`), additive; new runtime module | for every active loan with an unpaid `loan_installments` row due before the loan-local day, one unit of work: the Reg X windows opened once per due date (`loan.delinquency.window_opened{due_date}` — arms REGX_1024_39A_LIVE_CONTACT_36 / 39B_WRITTEN_NOTICE_45), the day milestones once per day (`loan.delinquency.day_reached{day}`), the `loans` projection 13.1 reads (`earliest_unpaid_due`, `principal_residence`, `state`), then 13.1's `sweepDelinquencyCounters` (`delinquency.counters.updated`, the 120-day gate). Run by the 32.10 flow's tick (POST /v1/sweep). | 32.10-T1, T10: no bus tool ran the counters; the surface must never compute a delinquency day. |
| 11.3 `lossmit.request.create{imminent_default{…}}` → the 11.5 evaluation on the bus (`imminent_default.evaluating` / the full `evaluationEvents`; `imminent_default_evaluations` row) | 11.3/11.5 (`src/app/tools/section11.ts` `requestWithImminentDefault_11_5`), additive input | 11.5 registered no tool of its own; a current borrower's stated hardship opens the evaluation, which waits on the BRP (`awaiting_brp`). | 32.10-T2 |
| 11.3 `preference.set{cease=written\|oral, recipients?, payload?, regx_days_delinquent_at_boarding?}` → 11.4's `writtenCease` / `oralCease` on the loan's `fdcpa_status` (`fdcpa.cease.received{written}`, REGF_1006_6C_CEASE_GATE, the D2-2-02 cadence cancelled, `NTC_REGF_1006_6C_CEASE_ACK` rendered and sent once through the registry; `fdcpa_cease_acks`) | 11.3/11.4 (`section11.ts` `ceaseCommunication_11_4`), additive input | a typed "stop contacting me" is a written cease (the app is a written channel); no bus tool recorded one. | 32.10-T11 |
| 12.2 `lossmit.trial.first_payment_received{acceptance}` beside `accepted_by_payment` | 12.2 (`src/domain/lossmit/ops-12-2.ts`), additive payload field | 12.8's FNMA_E3401_FC_SUSPEND_DURING_TRIAL triggers on `lossmit.trial.first_payment_received{acceptance=true}`; 12.2's event spelled it `accepted_by_payment` only. | 32.10-T6 (tpp_active) |
| 12.2 decision keeps the offer `terms` (+ `terms_source` = smdu / calculator / caller) on `lossmit_evaluations` | 12.2 (`src/app/tools/section12.ts`), additive | the offer ComparisonCard, the trial PaymentCard's default amount and the Loan section's trial line render the engine's figures, never recomputed. | 32.10-T5, T6 |
| 32.2 `lossmit.requestAssistance`: `hardship_reason` / `qrpc_id` count as evaluative information; `op` / `id` / `status` pass through; `changes{hardship_reason, qrpc_id, hardship_nature, hardship_text}` on the 12.1 row | 32.2 (`src/app/tools/section32-2.ts`), additive | a stated hardship with its reason is an application (12.1 `classify`), and the ConfirmCard's correction updates the same row. | 32.10-T2 |
| The cashiering → 12.2 trial bridge: `payment.received{designation=trial}` on a loan with a pending TPP offer → 12.2 `lossmit.evaluation.*{trial_payment_received}` as the lossmit agent | 32.10 flow (`src/runtime/borrower/flows/10-hardship.ts`), no owner change | 12.8's own trial milestones listen to 2.6's `trial_payment.*` events, which the portal receipt does not emit; the flow bridges the receipt to 12.2's acceptance-by-payment. | 32.10-T6 |
| PaymentCard evidence → command input (`amount_cents`, `date`, `account_id`, `include_late_charge`, a new account) in `cardArgs` | 32.2 API seam (`src/runtime/borrower/commands.ts`), additive branch | the PaymentCard's evidence IS the payment (01 §3.12); `payment.makeOneTime` reads them as its input. | 32.10-T6 |
| `agent_decisions.evidence_document_ids` (uuid[]) keeps only uuids | infra (`src/infra/db/decisions.ts`), additive guard | 13.1's `foreclosure.gates.evaluate` cites its `foreclosure_gate_evaluations` rows (`fge-…`) as evidence; the uuid column rejected them (its in-memory spec tests never hit the column). | 32.10-T4, T10 |
| Record: badges "Bankruptcy — protections in effect" / "Paused" / "On a plan" off the event spine; `loan.delinquency.day_reached{day}` read beside `n` / `days`; `needed_from_you` label / due from a card's `needed_label` / `needed_due_at`; the 4.3 team name from `continuity.assigned{team}`; `loan.hardship` (src/runtime/borrower/flows/10-hardship-record.ts); the 32.10 notice prefixes visible | 32.2 (`record.ts`, `serialize.ts`), additive | 32.10 §Record. | 32.10-T1, T3, T6, T7, T9 |
| `Journey.adoptPriorLoan(partyId, {first_unpaid_due, unpaid_months, fdcpa_debt_collector, regx_days_delinquent_at_boarding})` | test fixture (`src/runtime/borrower/fixtures/journey.ts`), additive | the servicing book's prior loan as the signed-in party's serviced loan (borrowers → loan_borrowers, loan_terms v1, the unpaid `loan_installments`). | every 32.10 T-id |
| Journey-driven facts the spec text states differently (recorded, not changed): (a) T5 "deemed_rejected on Nov 17" — 12.2 deems the rejection at accept_by + its 5-day policy grace (12.2 open question 1; `evaluationDeadlines`), i.e. Nov 21 for a Nov 2 offer; on Nov 17 the flow expires the ComparisonCard and files the StatusCard `hardship.offer.deemed_rejected` (the copy that said so on Nov 2), and the test asserts 12.2's own `lossmit.offer.deemed_rejected` on Nov 21. (b) T2 "`lossmit_applications` row is `received`" — 12.1's row status after receipt is `incomplete` (its own state name); `lossmit.application.received` is the event. (c) T4's 13.1 sale evaluation reads the case's `sale_on`; the firm's SALE_SCHEDULED writes `sale_scheduled_at`, so the dual-track gate closes on "complete application after first notice, no sale on the row" while 12.1's `lossmit.application.completed{days_before_sale=40}` carries the 40 days. (d) T10 "all state notices sent" — AZ names no `NTC_STATE_PREFC_*` notice (vacuous). | — | — |

## 11. Deltas the 32.11 build implemented (rate-watch and the re-refinance loop)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| DELTA-05 refresh/revocation on the command surface: `consent.capture{withdraw=true, kind, purpose?, consent_id?, reason?}` → `consents.status = withdrawn` (+ `revoked_at`, `withdrawal_channel = portal`, `withdrawal_reason`) and `consent.<kind>.withdrawn{consent_id, party_id, purpose, withdrawn_at, channel, reason, card_instance_id}`; `consent.granted` carries the loan subject when the consent is captured on a serviced loan | 32.2 (`src/app/tools/section32-2.ts`), additive branch + subject on the event | The standing authorization (`consents{kind=blanket_verification_authorization, standing=true}`) is offered after the first funding on the loan (`loan.boarded`) and is revocable from the Loan section through a ChoiceCard whose tap is a 32.2 command; the flow reacts to the loan-scoped `consent.granted` (the manage card) and to the withdrawal (T9). The refresh policy: the standing Truv connection (`FakeTruv`) is refreshed only in the conversion after the borrower's **Yes** (22.3 `orderVerificationReport{order, authorization_consent_id}` → `receive`), never for selection (20.1). |
| `application.confirmField{path=ssn_on_file}` → 21.1 `confirmPrefill{item=ssn, confirmed}` (the prior application's SSN on file, never re-typed) | 32.2 (`src/app/tools/section32-2.ts` `confirmFields`), additive case | The compressed application confirms identity on file (§3 table: "Identity: L3 already on file"); 32.3's `ssn` case is the typed nine digits and must not be re-asked (T6). |
| 21.1 `confirmPrefill{op=offer, item=ssn, on_file_value_hash}` — `offerPrefill(app, item, value, {on_file_value_hash})` takes the prior file's own six-item hash instead of a value | 21.1 (`src/domain/application/ops-21-1.ts`, `src/app/tools/section21-1.ts`), additive optional input | The platform keeps no SSN in clear (`application_borrowers.tin_encrypted` is never written; `tin_last4` and 21.1's `six_items.ssn.value_hash` are what the origination file holds); the compressed application's SSN prefill is that hash, confirmed item by item (21.1 rule 1) so `application.trid_received` fires on the sixth confirmation (T6). |
| 30.3 `buildEscrowLines{op=post_credit_transfer, custodial_ti_prepurchase_id}` — `postCreditTransfer({custodial_ti_prepurchase_id})` posts rule 8's second set to a named custodial account row | 30.3 (`src/domain/orig-boarding/ops-30-3.ts`, `src/app/tools/section30-3.ts`), additive optional input beside `fnma_ti_account_id` | On the Postgres ledger `custodial_accounts.id` is a uuid; the constant `custodial_ti_prepurchase` cannot post there. The same-servicer funding credits the old loan's escrow balance to the new loan (`escrow.credit_to_new_loan.posted`, `disbursement.issued{kind=payoff_refund, method=credit_to_new_loan}` satisfies `REGX_1024_34B_PAYOFF_ESCROW_REFUND_20BD` on the old loan's subject) (T7). |
| `offer.respond{never}`: the marketing consents are revoked as `status = withdrawn` + `revoked_at` + `withdrawal_reason = never_proactive_offers` (the 0076 `consents_status_check` admits no `revoked`); the event keeps the spec's word `consent.marketing.revoked` | 32.2 (`src/app/tools/section32-2.ts`), fix of a value the table rejects | **Never** on the OfferCard ended in a 500 (check violation); T5 reads the row (`withdrawn`, `revoked_at`, the reason) and the event. |
| Journey fixture: `assignMlo()` (21.1 `assignMLO` from a one-entry roster: Jordan Rivera, NMLSR 987654), `rescissionOverride` folded into `fund()`'s facts | test fixture (`src/runtime/borrower/fixtures/journey.ts`), additive | The OfferCard names the MLO of record (§2 attribution; `mlo.review.completed{approved}` is the MLO's act on the offer's terms — an inquiry lead `L-offer-<opportunity>` unlinked to the opportunity, a 20.4 `solvePassThrough` quote `Q-OFFER-<opportunity>` from 20.1's `candidateQuoteInputs`, 20.3 `request_review` → `review` → `present`); T8 funds under 25.3's `not_applicable` state. |
| UI: `RecordLoan.ratewatch{rate_sheet_id, state, worth_it_copy_key, state_copy_key, offer_card_instance_id, application_id}` + `RecordLoan.standing_connections`, `OfferCardProps{term_months, lender_nmlsr_id, path, expiry_timer_code}`, `StatusCardProps.copy_token_keys` (a token named by copy key), the OfferCard's §2 statements (`offer.payment_line`, `offer.no_cost_line` / `offer.costs_line`) and the Loan section's Rate-watch rows (`components/flows/11-rate-watch`) | 32.1/32.2 types (`apps/borrower/lib/types`), `OfferCard.tsx`, `StatusCard.tsx`, `record/sections.tsx`, additive | The block is passive until an opportunity exists; the funded StatusCard's autopay token is a copy key (`refi.autopay.carried_over` · `reauthorize` · `none`), never a sentence in props (T3, T5, T7). |

Observed, not changed: 20.1 `emitOfferReady{op=engaged}` takes the *first* `lead.created{opportunity_id}` on the loan as the engagement (the flow therefore never links its MLO-review inquiry lead to the opportunity; the borrower's own lead from `offer.respond{yes}` is the one 20.1 sees). 20.3 rule 6 keeps a lead's `income` TRID item to the pre-conversion statuses, so the compressed application's income ConfirmCard carries no `lead_id` — 21.1 owns the six items after conversion. The test book moves with the calendar (`payments_made` / `next_due_date` / `upb_cents` on the 20.1 universe row) because 20.1's payoff estimate accrues from `next_due_date`; a stale row in March 2027 fails the 84-month benefit test on its own arithmetic.

## 12. Deltas the 32.8 build implemented (servicing: loan home, payments, autopay, statements, escrow)

| Delta | Owner touched | What changed | Why the borrower surface needed it |
|---|---|---|---|
| 2.1 `payments.read/write{op=post, id, loan_id, state, custodial{clearing,pi,ti}, days_delinquent?, commitment_kind?}` — the posting run for one received payment: the Allocation Engine's plan, the rule-8 entry sets (receipt · allocation · cash split) through the unit of work's ledger, the row `posted` with its allocation and `ledger_entry_set_ids`, `payment.applied` per installment, `payment.posted{installments, interest/principal/escrow/late_charge_cents, upb_after_cents}`; n = 0 → 2.2 rule 3 (`decidePartial`, a portal submission is a `portal_note` commitment) → `suspense_items` `open` + `suspense.item.created{partial_payment}`, the row `held` with `balance_needed_cents` | 2.1/2.2 (`src/app/tools/section2-1.ts` `postReceivedPayment`, wired from `section02.ts`), additive op | The borrower's PaymentCard submission (`payment.received{channel=portal}`) had no path from `received` to `posted`; the Thread's *received → posted* receipt and the *held* line (T3) are the engine's outcome, never the UI's. | 32.8-T1, T3, T4, T6 |
| 2.2 `suspense.read/write{op=refund, id, loan_id, refunded_on, requested_by, data{action=return, payee_is_borrower, lossmit_case_active, amount_cents}}` → `closeSuspenseItem(refunded)` (`suspense.item.closed{outcome=refunded}`), the receipt entry set reversed, the payment row `refunded`, `payment.refunded{…}` | 2.2 (`src/app/tools/section2-2.ts` `withRefund`), additive op | 32.8 §3.3 "the borrower can always ask for the funds back (`refunded`)": the flow's refund ChoiceCard → `case.open{REFUND-<item>}` → the cashiering agent's own tool. | 32.8-T3 |
| 2.3 lifecycle on the bus: `autodraft.read/write{op=authorize\|validate\|settle\|return\|amount_change_check}` — `authorize{authorization, channel, recipients}` (CONSENT_VOICE_VOID / AUTH_ELEMENTS_MISSING refusals, `AUTODRAFT-CONFIRM-v1` rendered + sent through the registry, `notice.sent{enrollment_confirmation}`), `validate{status, next_draft_on}`, `settle{amount_cents, settlement_date, installment_due_date, next_draft_on}` (the debit as a *received* 2.1 payment the posting run allocates; `last_debit_cents`), `return{code, returned_on, original_entry_on, payment_id, …, recipients}` (rule 7: `ach.return.received`, `payment.reversed` + the payment's entry sets reversed, `ach.entry.reinitiation_scheduled{retry_on = +3 federal banking days}`, `AUTODRAFT-RETURN-v1`, `suspended_returns` on the second return), `amount_change_check{next_amount_cents, debit_on, today, statement?{template, sent_on, amount_cents, debit_on}, recipients}` (rule 5: satisfied by a statement that stated the exact amount and date, else `AUTODRAFT-AMOUNT-CHANGE-v1` ≥ 10 days before, else `autodraft.entry.held`) | 2.3 (`src/app/tools/section2-3.ts` `withAutodraftLifecycle`, wired from `section02.ts`), additive ops; the enrollment row kept in `autodraft_enrollments` | 2.3 registered only the generic read/write; the ConsentCard → `authorized → validating → active`, the return handling and the Reg E variable-amount notice are the owning process's acts the flow and the daily sweep call. | 32.8-T4, T5, T6 |
| 2.7 `fees.assess{op=daily_run, state, run_on, facts{items_received_or_identified_on_or_before_gate_date}, unposted_receipts_on_or_before_grace}` → `LateChargeOps.dailyRun` (`installment.due_date_reached{grace_end_on}` on the due date; the assessment the day after the engine's grace end → `fee.assessed{late_charge}`); the fee kept in `fees` | 2.7 (`src/app/tools/section02.ts`), additive op; the `2.1.noPostingBacklog` gate on the run's own facts | The badge's *Payment due* / *Past due* and the PaymentCard's date options are 2.7's `grace_end_on` and its assessment, never a computed date. | 32.8-T1 |
| `notice.send` / 3.x `sendNotice` without `notice_id` → `render_send` (`{template_code, recipients, payload}`); `sendNotice` records `escrow.statement.sent{stated_payment_cents, stated_payment_effective_on}` from the statement's `new_payment_cents` / `new_payment_effective_on` (3.3 `recordStatementSent(..., stated_payment)`) | 2.x/3.x (`section02.ts`, `section03.ts`, `src/domain/escrow/ops.ts`), additive | The Notice Registry lives in the unit of work: a render in one command is not sendable by the next. The statement's item (i) figure is what 2.3 rule 5 compares the changed debit against. | 32.8-T6, T8, T9 |
| 7.4 `recordBounceSuspect(deps, {loan_id, party_id, consent_id, notice_id, template, bounced_at, fallback_mailed_at})` → `consent.esign.suspect{reason=hard_bounce, mail_until_reverified, reinvite=NTC_ESIGN_VERIFICATION_EMAIL}` | 7.4 (`src/domain/notices/ops-7-4.ts`), additive | Rule 8's suspect fact for the flow's re-verification ConsentCard and the Record's *Mailed* row. | 32.8-T7 |
| `src/runtime/servicing.ts` (new): `loanCashState(rt, loanId, asOf)` (the 2.x `LoanCashState` from `loans`, `loan_terms` + the 3.6 `loan_terms` version, the ledger, the payment and fee rows), `servicingParties` / `recipientsOf` (parties + `consents{esign, irs_estatement}`), `servicingDailySweep` (2.1 posting of received payments, the 2.7 run on due dates and the day after a grace end, the 2.3 amount check inside 31 days of a draft), `sendPeriodicStatement` (7.1: the availability e-mail `NTC_REGZ_41_STMT_AVAIL_EMAIL` per active consent, a hard bounce → 7.4 rule 8 + `consents.status = suspect`, `NTC_REGZ_41_STMT_STD` rendered/sent, the FAKE print/mail production the same day → `statement.sent{mailed_at}`, `statement.bounced` / `statement.fallback_mailed`), `furnishForm1098` (7.1-A: `requestForm1098Furnish` gate → paper without `irs_estatement`, box 1/2 from the ledger, `tax_form.1098.furnished{channel}`) | runtime module (no owner change); the flow's `tick` runs the sweep | 7.1 / 7.4 / 7.1-A register no bus tools and the statement cycle needs the loan's cash state; the FAKE `servicer` contact block is named `SERVICER_CONTACT`. | 32.8-T1, T6, T7, T11 |
| The runtime's notice registry publishes 2.x's own authored pieces (`publishSection02`: AUTODRAFT-*, LC-*, SUSP-*) beside the catalog | runtime (`src/runtime/app.ts`), additive | `AUTODRAFT-CONFIRM-v1` / `-RETURN-v1` / `-AMOUNT-CHANGE-v1` are authored in `src/notices/authored/section02.ts` but not in `spec/registry/notices.json`; the bus tools rendered "unknown notice template". | 32.8-T4, T5, T6 |
| 32.2: `escrow.electShortage{spread_12}` → 3.6 `recordBorrowerElection` (the election under `data`, the resolved card as its evidence) + `createRepaymentPlan{kind=shortage, analysis_id, start}` (12 installments, `loan_terms` versioned); `escrow.requestWaiver` guarded by `REGZ_1026_35B1_HPML_ESCROW_GATE` (HPML from the request / `compliance.hpml.determined` / `closing.consummated`, consummation from `closing.consummated` / `loan.boarded`; refused before consummation + 5y with copy `escrow.waiver.hpml_period`) and the request's cents parsed for 3.8; `autodraft.enroll/change` keep `party_id` and `account.routing` | 32.2 (`src/app/tools/section32-2.ts`, `src/runtime/borrower/copy-keys.ts`), additive | 32.8 §6.2 "→ plan `active`"; §6.3 "HPML loans stay escrowed ≥ 5 years" as the API's own refusal {code, gate, copy_key} (T10); the ConsentCard goes to the party who enrolled (T5). | 32.8-T5, T8, T10 |
| Record: servicing badges *Current* / *Payment due* / *Past due* from `installment.due_date_reached{grace_end_on}`, `payment.posted` (a payment a later `payment.reversed` voided no longer counts) and `fee.assessed{late_charge}` with `status.one_liner_tokens` (serializer: opaque); Documents rows carry `notice_id`, a bounced piece reads *mailed* with the fallback's production date; `loan.year_end{form_1098_status = mailed \| available \| pending, tax_year, furnished_on, channel}` from `tax_form.1098.*` | 32.2 projection (`src/runtime/borrower/record.ts`, `serialize.ts`), additive | 32.8 §2 badge table and §5 year-end block. | 32.8-T1, T7, T11 |
| Journey fixture: `board(snapshotOverrides)`, `postInstallment(dueDate, receivedOn, {amount_cents?, channel?})` (2.1 write + `op=post`) | test fixture (`src/runtime/borrower/fixtures/journey.ts`), additive | The T-ids drive installments through the 2.1 bus tools, never by inserting rows. | 32.8-T1, T3 |
| UI: `PaymentCardProps{copy_tokens, installment_due_date, grace_end_on}` and the PaymentCard titled from its copy key (`payment.due` / `payment.another_way`), resolve `args{amount_cents, date, account}` beside the evidence; `ChoiceCard` heading/helper from the copy key with tokens; `StatusCard` / `NoticeCard` / `ChoiceCard` `copy_tokens` values may be arrays (a token used twice); `RecordStatus.one_liner_tokens`, `RecordLoan.year_end{tax_year, furnished_on, channel}`; `components/flows/8-servicing-payments` (`paymentTitle`, `autopayElementsMissing`, `form1098Label`) | apps/borrower (additive) | 32.8's cards name copy keys with tokens; the Loan section's *Mailed {{date}}* 1098 row (T11). | apps/borrower/tests/cards/flow-8-servicing-payments.test.tsx |

Journey-driven facts the spec text states differently (recorded, not changed): (a) T1 "Past due from Oct 16" — 2.7's grace end for an Oct 1, 2027 due date is Sat Oct 16 rolled to Mon Oct 18 (2.7 decision 2, the servicer calendar) and the assessment runs the day after the grace end, so the badge reads *Payment due* through Oct 18 (grace end shown) and *Past due* from the Oct 19 run; the January installment's grace (Sat Jan 16, 2027) rolls past MLK Day to Tue Jan 19. (b) T3 "$1,000 against a $2,400 installment … the remaining $1,400" — the journey's installment is $4,090.12 (P&I $3,402.62 + escrow $687.50), so the held line names $3,090.12; the figures are the loan's, not the spec's worked example. (c) T4 "3–5 banking days" — the platform's reinitiation date is `addBusinessDays(returned_on, 3, federal)` (Tue Jan 4 → Fri Jan 7, 2028). (d) T7 — the statement's own delivery to the bounced party is the FAKE print/mail production run the same day; a real vendor manifests overnight. (e) T11 — the 1098's box 2 is the ledger's principal at Jan 1 ($560,000.00: the first installment posted Jan 12, 2027).

## Open follow-ups after the 32.3–32.13 build (recorded, not changed)

Each of these is worked around by the flow or test layer today; none affects a counted spec unit. They are listed so the next pass starts from the platform, not from the symptom.

| Item | Where | Why it is open |
|---|---|---|
| A resolved card's flows can observe the card as `pending` | `src/runtime/borrower/commands.ts` `resolveCard`: the card's command runs on the bus (its own unit of work commits, and the post-commit hook fans the events to the flows) before the enclosing transaction marks the card `resolved`. | A flow reacting to the command's events and keyed on the card's status sees the pre-transition row during that window. Flows built here key on the events, not the status, so the window is harmless; a future flow that reads the status must settle first. |
| `withDefaultLoan` stamps the unit of work's loan, not an event's explicit application | `src/runtime/app.ts` `withDefaultLoan`: events written through a loan-scoped unit of work carry that loan id; an event whose payload names an application explicitly is not re-keyed. | 32.11 (rate watch) needed both ids on `lock.expiry.warned` after boarding; the origination sweep now skips the lock playbook once `applications.loan_id` is set (below) rather than re-keying. |
| `SM_LOCK_EXPIRY_WARN_7` stays armed after funding | `src/runtime/origination.ts` lock-expiry sweep selects only applications with `loan_id IS NULL`; the timer row itself is not satisfied or cancelled at funding. | 21.4's registry row has no `satisfied_by` for `loan.funded`; adding one is a spec registry question for 21.4, not a runtime change. Until then the armed row is inert. |
| `document.upload` has no servicing path | `src/app/tools/section32-2.ts` `document.upload` always routes to 22.1 `ingestDocument{source_channel=borrower_upload}` on the application. | A servicing-stage upload (a hardship package under 32.10, an insurance declaration under 9.x) is delivered by those processes' own commands today; a single `document.upload` that resolves the subject's stage and routes to 12.x/9.x would remove that split. |
| 32.11's compressed application named a foreign lead (fixed) | `src/runtime/borrower/flows/11-rate-watch.ts` `leadOf(ctx)`: the application's own lead by id, by `application_id`, by party, or the offer's `L-offer-<opportunity>` on the prior loan. | The flow took the first `leads` row of the application-scoped store. That store also holds every global row, and a pre-application lead is global until 20.3 links it, so on any database with unlinked leads the ConsentCards `consent.credit.title` / `consent.esign.extend` carried another consumer's `lead_id`, 20.3 recorded the consent on that lead, and the demonstration step answered "no esign consent on lead". Found by the full suite on the shared test database (14 global leads from the 20.x suites; 52 of 94 credit cards and 52 of 86 E-SIGN extension cards foreign) after every per-database run had passed. |
| Vendor fakes | `src/runtime/borrower/vendors/*` and the integrations layer: Stripe Identity, Truv, Plaid, IRS IVES, carrier connection, RON/eClosing, DU, EarlyCheck, telephony/SMS/e-mail, print/mail, AMC, blob store are in-repo fakes named `FAKE`. | Every one is a port with a fake adapter; a real adapter is a new file behind the same port, never an edit to a flow. |

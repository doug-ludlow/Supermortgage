/**
 * §16.1 timer overrides: for every 16.1 registry row whose "Satisfied by" (or trigger/anchor/offset) column is prose, a
 * `reg.override(code, { satisfied | evaluator, trigger?, offset?, anchorField?, why })` (see
 * src/domain/foreclosure/timers.ts applyForeclosureSatisfiedOverrides for the pattern). Called from this section's
 * timers.ts after the section-level overrides; every event named here is one the 16.1 tools (src/app/tools/section16-1.ts)
 * or the A4-2.1-07 sweep (`nibMaturitySweep`) emit:
 *   `payoff.request.received{written, requester, statutory_statement_due, received_on, good_through}` — `computePayoffQuote` records
 *     the written request through `payoffRequestIntake` (ops-16-1.ts) once per request id before the engine's first run;
 *   `payoff.quote.requested{mode}` — `computePayoffQuote` records an oral/portal/API/internal quote request (`quoteRequestIntake`);
 *   `payoff.quote.computed{quote_type, oral}` — every computed figure; `payoff.statement.sent{updated, all_prior_recipients}` and
 *     `payoff.statement.delivered{updated, state}` — `sendNotice`; `payoff.statement.updated{state, payment_date}` — `scheduleRecompute`;
 *   `notice.render_requested{template}` — `renderStatement`; `payoff.request.third_party.resolved{outcome}` — `computePayoffQuote` /
 *     `sendNotice`; `loan.maturity.approaching{days_before, nib, contact}` — the sweep; `notice.sent{template, sequence}` and
 *     `payoff.nib_maturity.resolved{outcome}` — `sendNotice` / `recordDecision`.
 * `TimerRegistry.override` recomputes `anchorField` from the raw row, so a computed anchor is re-stated here wherever a
 * satisfaction override lands on the same code.
 */
import type { TimerRegistry } from "../../kernel/timers/registry.ts";

export function applySatisfiedOverrides_16_1(reg: TimerRegistry): void {
  const o = reg.override.bind(reg);
  o("REGZ_1026_36C3_PAYOFF_STMT_7BD", { anchorField: "received_on",
    why: "§16.1 timer table (owned by 7.6): anchor 'receipt date' — 7.6's anchor column reads 'receipt date (email/fax/portal: same day; mail: vendor receipt date)'; `payoffRequestIntake` applies 7.6's `payoffClockStart` and writes that clock-start date into the `payoff.request.received{written}` payload as `received_on`, so the +7 `business_days_servicer` clock runs from the receipt date rather than from the moment the engine recorded the request (example A: e-mailed Mon 09/14 → day 7 = Wed 09/23). Satisfied by `payoff.statement.sent` from `sendNotice`; breach sev-1, §1024.35(b)(6) exposure." });
  o("STATE_PAYOFF_STMT_DEADLINE", { trigger: "`payoff.request.received{written=true, statutory_statement_due is not null}`", anchorField: "statutory_statement_due", offset: "0", satisfied: "`payoff.statement.sent`",
    why: "§16.1 timer table: per `jurisdiction_rules.payoff.deadline_days`/`deadline_calendar` (CA 21 CD, FL 10 CD, NY 30 CD, NC 10 CD, MA 5 BD, TX 7 BD, CT on/before the requested date if ≥ 7 BD after receipt, else 7 BD) — `computePayoffQuote` records the written request through `payoffRequestIntake` (ops-16-1.ts, over `statutoryStatementDue` / `statementDeadlines`), which writes the date into the `payoff.request.received{written}` payload as `statutory_statement_due`; null where no state deadline was located (Ohio), so only the federal 7-BD clock arms; the earliest of federal and state drives the 70% warning and breach (sev-1; state exposure log CA $300, MA $500, CT interest forfeiture — `statementDeadlineBreach` via `escalate{reason_kind=statement_deadline_breach}`)." });
  o("SM_PAYOFF_ORAL_QUOTE_SAME_SESSION", { satisfied: "`payoff.quote.computed{quote_type=oral}`",
    why: "§16.1 timer table: '`payoff.quote.computed{oral}`' — the engine spells the quote type as `payoff_quotes.quote_type` (data model: ∈ {statement, oral, portal, internal, updated}); `computePayoffQuote{quote_type=oral}` records `payoff.quote.requested{mode=oral}` (`quoteRequestIntake`, after identity verification — rule 11) and, in the same call, emits `payoff.quote.computed{quote_type=oral, oral=true}`, so the figure is given during the call/chat (offset 0); breach sev-3 → callback within 1 BD." });
  o("SM_PAYOFF_PORTAL_QUOTE_60S", { offset: "+1 minutes", satisfied: "`payoff.quote.computed{quote_type=portal}`",
    why: "§16.1 timer table: '60 seconds' from the `payoff.quote.requested{mode=portal}` request (`computePayoffQuote{quote_type=portal}` → `quoteRequestIntake`) — the offset grammar's smallest unit is the minute, so the SLA is stated as +1 minute (= 60 seconds; a bare '60 seconds' would parse as a day-of-month); 'quote displayed' — the portal displays the figure from the `payoff.quote.computed{quote_type=portal}` the engine emits in the same call (rule 11 / `payoff_quotes.quote_type`); breach sev-4 → fallback message (`portalQuoteSla`)." });
  o("SM_PAYOFF_STMT_UPDATE_1BD", { satisfied: "`payoff.statement.sent{updated=true, all_prior_recipients=true}`", why: "§16.1 timer table: '`payoff.statement.sent{updated}` to all prior recipients' — `sendNotice` of `NTC_PAYOFF_UPDATED_STMT` compares the recipients with the original statement's `delivered_to` (`updatedStatementRecipients`) and emits `all_prior_recipients=true` only when every prior party/channel is covered (comment 36(c)(3)-3; sev-2); armed by `scheduleRecompute`'s `payoff.quote.recompute{delta ≠ 0}` on an active statement (the recompute runs the calculator on the post-event ledger facts and writes the `updated` payoff_quotes row)." });
  o("FL_701_04_CORRECTED_ESTOPPEL_CUTOFF", { trigger: "`payoff.statement.updated{state=FL}`", anchorField: "payment_date", offset: "−1 business_days_servicer, 15:00 ET", satisfied: "`payoff.statement.delivered{updated=true, state=FL}`",
    why: "§16.1 timer table: 'updated statement' → `scheduleRecompute` emits `payoff.statement.updated{state, payment_date}` for the Δ ≠ 0 recompute; the corrected estoppel letter must be *received* by 3 p.m. (Eastern — Florida) at least 1 BD before the payment date to supersede (Fla. Stat. §701.04(1)); 'send + delivery evidence' — `sendNotice` with `delivery_evidence_document_id` emits `payoff.statement.delivered{updated=true, state=FL}`; after the cutoff the prior figure is honored and the servicer absorbs (`flCorrectedEstoppel`)." });
  o("FNMA_A42107_NIB_MATURITY_NOTICE_180_150", { trigger: "`loan.maturity.approaching{days_before=180, nib=true}`", anchorField: "anchor_date", satisfied: "`notice.sent{template=NTC_FNMA_NIB_BALANCE_NOTICE, sequence=first}`",
    why: "§16.1 timer table / schedules: the nightly A4-2.1-07 sweep (`nibMaturitySweep`) emits `loan.maturity.approaching{days_before=180, nib=true}` with `anchor_date` = the maturity date or the projected date of payoff, whichever is first; '`NTC_FNMA_NIB_BALANCE_NOTICE` sent' — `sendNotice` of the first Borrower Notification of Non-Interest-Bearing Balance emits `notice.sent{template, sequence=first}` 180→150 CD before the anchor (A4-2.1-07; `nibMaturityNotices`)." });
  o("FNMA_A42107_NIB_MATURITY_NOTICE_75_60", { trigger: "`loan.maturity.approaching{days_before=75, nib=true, contact=false}`", anchorField: "maturity_date", satisfied: "`payoff.nib_maturity.resolved{outcome∈{second_notice_sent, contact_established}}`",
    why: "§16.1 timer table: 'no contact by maturity − 75' → the sweep emits `{days_before=75, nib=true, contact=false}` only for loans without an established contact; 'second notice sent or contact established' — `sendNotice` of the second `NTC_FNMA_NIB_BALANCE_NOTICE` emits `payoff.nib_maturity.resolved{outcome=second_notice_sent}` and `recordDecision{action=nib_maturity.contact_established}` emits `{outcome=contact_established}`; either satisfies the 75→60 CD window before maturity (A4-2.1-07; sev-2)." });
  o("SM_PAYOFF_WIRE_VERIFY_GATE", { trigger: "`notice.render_requested{template=NTC_REGZ_36C3_PAYOFF_STMT}`", evaluator: "16.1.wireInstructionsVerified",
    why: "§16.1 timer table: 'statement render' → `renderStatement` emits `notice.render_requested{template=NTC_REGZ_36C3_PAYOFF_STMT}` (the authored 7.6 code, spec/registry/notices.json) carrying the gate facts (`wire_instruction_version_id`, `active_vault_version_id`, `verification_token`, `statement_hash`, the minted-token binding) before its gates run; wire instruction version = active vault version and the verification token minted for this statement hash on that version (`wireVerifyGate` over the `mintVerificationToken` record); anything else blocks the render and is a fraud signal to `security-records`." });
  o("SM_PAYOFF_THIRD_PARTY_AUTH_1BD", { trigger: "`payoff.request.received{written=true, requester=third_party}`", anchorField: "received_on", satisfied: "`payoff.request.third_party.resolved{outcome∈{verified, authorization_request_sent}}`",
    why: "§16.1 timer table: 'third-party request' — `payoffRequestIntake` classifies every requester other than the borrower or a confirmed successor (`requesterAuthorization`, 7.6) as `requester=third_party` in the `payoff.request.received{written}` payload, anchored on the receipt date (`received_on`); 'verified or authorization request sent' within +1 BD (7.6 `SM_PAYOFF_REQUESTER_VERIFY_1BD`) — `computePayoffQuote` resolves `verified` on a requester with authorization evidence; `sendNotice` of `NTC_PAYOFF_AUTHORIZATION_REQUEST` resolves `authorization_request_sent` (`thirdPartyRequest`)." });
}

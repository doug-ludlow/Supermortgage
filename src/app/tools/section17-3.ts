/**
 * §17.3 tools — the spec's tool strings for process 17.3, verbatim, via
 * `defineTools("17.3", "<agent>", defs)` from ../tools.ts (see section13.ts). Spread by ./section17.ts.
 */
import type { ToolDef } from "../tools.ts";

export const TOOLS_17_3: readonly ToolDef[] = [];
